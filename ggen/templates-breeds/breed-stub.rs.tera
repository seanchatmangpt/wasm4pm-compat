// Breed stub: {{ breed_id }} — generated skeleton, implement and remove this comment.
// Citation: {{ citation }}
use crate::breeds::{BreedError, BreedId, BreedInput, BreedOutput, CognitionBreed};
use crate::breeds::support::tracer::Tracer;

pub struct {{ breed_label }};

impl CognitionBreed for {{ breed_label }} {
    fn id(&self) -> BreedId {
        BreedId::{{ breed_label }}
    }

    fn preconditions(&self, _input: &BreedInput) -> Result<(), String> {
        Err("breed not yet implemented".to_string())
    }

    fn postconditions(&self, _input: &BreedInput, _output: &BreedOutput) -> Result<(), String> {
        Err("breed not yet implemented".to_string())
    }

    fn run(&self, _input: &BreedInput) -> Result<BreedOutput, BreedError> {
        let mut trace = Tracer::new();
        trace.push("stub", "NOT YET IMPLEMENTED");
        let _ = trace;
        Err(self.error("breed not yet implemented".to_string()))
    }
}
