{# paper-ledger-row.tera: Renders a row for the PAPER_COVERAGE_LEDGER.md. #}
{# Constitutional Equation: ledger row = mu(wasm4pm-compat.ttl, PaperCoverage) #}
{# Input variables: #}
{#   citeKey          — BibTeX citekey, e.g. "vanderaalst2022ocel" #}
{#   title            — full paper title #}
{#   year             — publication year #}
{#   status           — one of: COVERED | PARTIAL | GAP | PENDING #}
{#   coveringModule   — comma-separated module names that cover this paper, e.g. "ocel, admission" #}
{#   graduationReason — why/whether any types graduate; empty string if none #}
{#   notes            — free-text notes, brief #}
{# Output: single markdown table row #}
{# Auto-generated by ggen from wasm4pm-compat.ttl --- DO NOT EDIT #}
| `{{ citeKey }}` | {{ title }} ({{ year }}) | {% if status == "COVERED" %}COVERED{% elif status == "PARTIAL" %}PARTIAL{% elif status == "GAP" %}GAP{% else %}PENDING{% endif %} | `{{ coveringModule }}` | {% if graduationReason %}{{ graduationReason }}{% else %}—{% endif %} | {% if notes %}{{ notes }}{% else %}—{% endif %} |
