# Historical WIT Artifact Manifest

This inventory preserves the June 1, 2026 WIT design inputs and intended output families.
It is not the active ggen manifest and must not be interpreted as one.

## Authority boundary

- Active consumer manifest: repository-root `ggen.toml`
- Active template inventory: `packs/wasm4pm-compat-pack/templates/*.tmpl`
- WIT template inventory in that pack: none
- Current WIT standing: `UNSUPPORTED`

Absolute workstation paths, claimed output counts, and “production-ready” language in the
original report were not executable receipts. The design may be re-used as admitted input
for a future ontology/frontmatter/consumer implementation, but it cannot bypass the pack,
graph gates, checksum ownership, receipt verification, or replay law.

See `WIT_TEMPLATE_USAGE.md` for re-admission requirements.
