/target

# Claude Code / agent runtime artifacts
.claude/
.agents/
*.rlib
conformance_verdict_is_perfect

# ggen/emitted/ — rendered artifacts (not source)
ggen/emitted/*.yaml
ggen/emitted/*.md
ggen/emitted/*.rs
ggen/emitted/*.wit
ggen/emitted/*.sh
ggen/intel/

# Agent-generated checkpoint and emitted directories
# (already tracked entries must be committed; this blocks future additions)
checkpoints/*.md
emitted/*.yaml
emitted/*.md
emitted/audit-machinery/
emitted/audits/

# Root-level agent receipt/gap docs (tracked; prevents future additions)
GAP_*.md
ORIGINAL_REQUEST.md
FINAL_*.md

# macOS files
.DS_Store
**/.DS_Store

# LSP build targets
/target-lsp
/target_lsp


# ggen Ed25519 receipt-signing keys — NEVER commit (auto-regenerated by ggen sync)
.ggen/keys/

# ggen incremental render snapshots — never source, never shipped
*.backup

# TS sidecar / node tooling — never enters git or the crate tarball
**/node_modules/

# Anti-cheat OCEL audit telemetry (runtime event log)
.ggen/ocel/

# Anti-cheat OCEL audit telemetry (runtime-generated, never shipped)
ocel/anti_llm_cheat_*.json
ocel/anti_llm_cheat_*.receipt.json
