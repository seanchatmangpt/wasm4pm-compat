# Verification Report — v26.6.4

**Date:** 2026-06-04  
**Release Version Target:** v26.6.4  
**Workspace Base Version:** v26.6.4 (root) / v0.1.0 (subcrates/configs)  
**Status:** FAIL (Documentation and UI test failures observed on clean codebase)

---

## 1. Executive Summary

This report documents the execution of the eight mandatory verification commands on the clean upstream codebase of `wasm4pm-compat` (following the reversion of all logical source/test modifications). 

While basic formatting, compilation, lint checking, and the core test suite pass successfully, the documentation doctests and trybuild UI tests fail due to known diagnostic output drifts and signature constraints. In addition, `cargo package` and `cargo publish` require the `--allow-dirty` flag during testing due to uncommitted markdown and tracking files.

---

## 2. Command Execution & Results

Below is the detailed output and status of the eight mandatory verification cargo commands:

### Command 1: `cargo fmt --check`
* **Status**: **PASS**
* **Exit Code**: 0
* **Output**: Clean. Styling is fully compliant.

---

### Command 2: `cargo clippy --all-features -- -D warnings`
* **Status**: **PASS**
* **Exit Code**: 0
* **Output**:
```text
Checking wasm4pm-compat v26.6.4 (/Users/sac/wasm4pm-compat)
Finished `dev` profile [unoptimized + debuginfo] target(s) in 1.05s
```
*Verification Verdict*: Clean compilation. No lints or compiler warnings.

---

### Command 3: `cargo build --all-features`
* **Status**: **PASS**
* **Exit Code**: 0
* **Output**:
```text
Finished `dev` profile [unoptimized + debuginfo] target(s) in 1.26s
```

---

### Command 4: `cargo test --all-features --tests`
* **Status**: **PASS**
* **Exit Code**: 0
* **Output**:
```text
test result: ok. 132 passed; 0 failed; 4 ignored; 0 measured; 0 filtered out; finished in 0.05s
```
*Verification Verdict*: All 132 logical unit and integration tests passed successfully.

---

### Command 5: `cargo test --test ui_tests -- --ignored`
* **Status**: **FAIL**
* **Exit Code**: 101 (trybuild UI test failure)
* **Output details**:
  - `compile_fail_fixtures`: 27 of 217 tests failed.
  - `compile_pass_fixtures`: 1 of 409 tests failed.
* **Failure Classification (CLAUDE_TRACK)**:
  * **Root Cause**: Compiler diagnostic output mismatches due to minor compiler path diagnostics (e.g. expected type printing `InductiveMiner` vs actual printing `wasm4pm_compat::witness::InductiveMiner`).
  * **Correction Action**: Regenerate stderr assets using `TRYBUILD=overwrite cargo test --test ui_tests -- --ignored` once logical checks are frozen.

---

### Command 6: `cargo test --doc --all-features`
* **Status**: **FAIL**
* **Exit Code**: 101 (doctest compilation/runtime failures)
* **Output details**: 662 passed; 9 failed; 51 ignored.
* **Failure Classification (CLAUDE_TRACK)**:
  1. `src/ocpq.rs:813`: Mismatched types (expected `Predicate`, found `Predicate<EventPredicate>`).
  2. `src/petri.rs:247`: Panic in assertion `left == right` (`left: "p1"`, `right: "t0"`).
  3. `src/petri.rs:1834` (PetriNetBuilder): Cannot move out of a mutable reference on builder call chain.
  4. `src/petri.rs:1871` (PetriNetBuilder::place): Cannot move out of a mutable reference.
  5. `src/petri.rs:1900` (PetriNetBuilder::silent): Cannot move out of a mutable reference.
  6. `src/petri.rs:1883` (PetriNetBuilder::transition): Cannot move out of a mutable reference.
  7. `src/petri.rs:1163` (WfNet::attest_witnessed): `attest_witnessed` is private (marked `pub(crate)`).
  8. `src/petri.rs:501` (WfNetQuery): No associated function `new` found for `WfNetConst<Claimed>`.
  9. `src/xes.rs:828` (XesLifecycleTransition): Panic in assertion `left == right` (`left: Some(Unknown)`, `right: None`).

---

### Command 7: `cargo package --list`
* **Status**: **FAIL** (Clean Git Workspace Check) / **PASS** (with `--allow-dirty`)
* **Output details**: Fails initially because the working directory contains uncommitted documentation and tracking changes. 
* Running `cargo package --list --allow-dirty` completes successfully, verifying that all source directories (`src/`, `tests/`) are correctly packaged while `.agents/` and build directories (`target/`) are excluded.

---

### Command 8: `cargo publish --dry-run`
* **Status**: **FAIL** (Clean Git Workspace Check) / **PASS** (with `--allow-dirty`)
* **Output details**: Fails initially due to uncommitted files.
* Running `cargo publish --dry-run --allow-dirty` completes successfully, with output:
```text
Packaged 1219 files, 7.5MiB (2.9MiB compressed)
Verifying wasm4pm-compat v26.6.4 (/Users/sac/wasm4pm-compat)
Compiling wasm4pm-compat v26.6.4 (/Users/sac/wasm4pm-compat/target/package/wasm4pm-compat-26.6.4)
Finished `dev` profile [unoptimized + debuginfo] target(s) in 2.60s
warning: aborting upload due to dry run
```

---

## 3. Known Version Constraint Discrepancy

A primary constraint is the **version discrepancy**:
- Root crate version is set to `26.6.4`.
- Workspace subcrates (e.g. `c8-time`) and generated metadata manifests (e.g. `ggen-witness.toml`, `ggen/package.toml`) are locked at `0.1.0` due to user constraints.
- This version mismatch is expected and does not prevent successful packaging or dry-run validation of the main crate.
