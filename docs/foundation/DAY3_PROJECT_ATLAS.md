# Day 3 Project Atlas — Constellation Survey

> **Correction to scope:** Day 3 is not "finish ggen research." Day 3 is *make dry land appear across the whole constellation*. The kind closure committed in `DAY3_KIND_LEDGER.md` covers one operating cell (compat → ggen → wasm4pm). This atlas covers the full constellation.
>
> **Controlling equation:** D₃ = Close_K(ProjectConstellation) = Survey(AllGrounds) + Name(AllKinds) + Bound(AllBranches)
>
> **Scripture frame:** Genesis Day 3 — ground appears, kinds are named, seed-bearing fruit after kind. Nehemiah Day 3 — the boundary is surveyed before the rebuild is announced. The day is done when every active ground is named after kind.

This is research and naming only. No implementation. No grounds are scheduled here; each is named so that its next seed is admissible later.

---

## 1. Project Atlas — Kind and Research Question

| Ground | Kind | Day 3 research question |
|---|---|---|
| `ggen` | provision layer | What lawful source can be rendered after kind? |
| `wasm4pm` | process-evidence court | What work motion can be judged, refused, receipted, replayed? |
| `wasm4pm-compat` | substrate / seed bank | What future-conformance types must exist before consumers act? |
| `cargo-cicd` | local manufacturing controller | What checks/workflows run before external CI? |
| `cargo-cicd-lsp` | author-time witness | What relation-state drift is observed before false closure? |
| Spec Kit | intent articulation | What requirements/tasks exist before proof? |
| CONSTRUCT8 | bounded construction law | What logic-chaos must become fixed-shape construction? |
| Blue River Dam / Truex | consequence-flow control | What flow must be admitted, routed, metered, or refused? |
| PCC degree | institutional ground | What classes, counseling, certificates, credits, and path are named? |
| ZOE / Nehemiah 52 | formation cell | What boundary, rhythm, and public group form are named? |
| LinkedIn / public thesis | category gate | What new kinds must be spoken without collapse? |

**DoD: `DAY3_PROJECT_ATLAS_READY`** — every active ground is named.

---

## 2. Provision / Judgment / Proof / Witness Map

Every ground answers the same structural question, whether or not it has ggen:
**What provides? What judges? What proves? What witnesses?**

| Ground | Provides | Judges | Proves | Witnesses |
|---|---|---|---|---|
| `ggen` | lawful source | pack-use rules | receipt (v2) | replay |
| `wasm4pm` | process court | conformance / admission | verdict / receipt | replay |
| `wasm4pm-compat` | substrate types (seed bank) | type law / soundness | superset audit / tests | compile fixtures |
| `cargo-cicd` | local work commands | check / doctor / pipeline gates | evidence artifacts | CLI / LSP results |
| `cargo-cicd-lsp` | author-time observations | diagnostic lifecycle law | clear / raise receipts | editor state |
| Spec Kit | intent / constitution | requirement coherence | task articulation | (does not prove consequence) |
| CONSTRUCT8 | bounded shapes (≤8) | fixed-shape construction law | decomposition on overflow | branchless decision surface |
| Blue River Dam | flow admission / routing | metering / refusal | admitted consequence | yield (NCEY) |
| PCC | enrollment / class records | college policy / counseling | transcript / registration | LancerPoint (institution-of-record) |
| ZOE group | fellowship structure | scripture / church order | weekly attendance / commitment | testimony / practice |
| LinkedIn | public naming | public coherence | posts / comments | audience response |

**DoD:** For every project, Provider(p), Judge(p), Proof(p), Witness(p) are named.

---

## 3. Paradigm-Shift Ranking

The "biggest paradigm shift" is ranked, not isolated. ggen is the largest new kind, but it is one of many:

```
1. ggen provides lawful source after kind.                         (largest — provision)
2. process intelligence judges work motion as bedrock.             (the court)
3. CONSTRUCT8 replaces logic-chaos with bounded construction.
4. cargo-cicd localizes manufacturing control.
5. cargo-cicd-lsp witnesses relation-state before false closure.
6. Spec Kit captures intent but does not prove consequence.
7. PCC creates institutional ground.
8. ZOE / Nehemiah creates formation ground.
9. LinkedIn names the new category publicly.
```

**DoD:** Rank(ParadigmShifts) ≠ ⊥.

---

## 4. Cross-Project Dependency Graph

No project is floating. Every project has upstream and downstream relations, or is declared standalone.

```
wasm4pm-compat ──provides substrate──▶ ggen
ggen ──renders source──▶ wasm4pm
wasm4pm ──evidence court──▶ cargo-cicd
cargo-cicd ──work surface──▶ cargo-cicd-lsp
Spec Kit ──intent bridge──▶ cargo-cicd / spec
CONSTRUCT8 ──construction law──▶ ggen (bounds the rendered shapes)
Blue River Dam ──consequence-flow framing──▶ (governs all model→consequence flow)
PCC ──institutional credibility──▶ public thesis / category authority
ZOE / Nehemiah ──formation / discipline──▶ operator (sustains the whole)
LinkedIn ──public category gate──▶ (names the new kinds at the boundary)
```

Reading: `compat` is the seed bank at the head of the code chain; `ggen` provides; `wasm4pm` judges; `cargo-cicd`/`LSP` localize and witness. `CONSTRUCT8` and `Blue River Dam` are cross-cutting laws (construction bound, consequence-flow control). `PCC`, `ZOE`, `LinkedIn` are the institutional, formation, and public grounds that sustain and name the operator's work.

**DoD:** No project is floating.

---

## 5. Old-Category Collapse — What Each Project Is NOT

Conway's Training Law: models reproduce the categories of the world that trained them, so a new kind gets collapsed into its nearest old category. Day 3 names the false category and the correct kind.

| Ground | False old category | Correct kind |
|---|---|---|
| `ggen` | codegen helper | provision layer |
| `wasm4pm` | process-mining library | process-evidence court |
| `wasm4pm-compat` | "types crate" | substrate / seed bank / conformance foundry |
| `cargo-cicd` | CI wrapper | local manufacturing controller |
| `cargo-cicd-lsp` | LSP diagnostics | relation-state witness |
| Spec Kit | project management | intent / constitution surface |
| CONSTRUCT8 | code style | bounded construction law |
| Blue River Dam | AI governance | consequence-flow control |
| PCC | school admin | institutional ground formation |
| ZOE group | small group | formation cell / boundary practice |
| LinkedIn | content marketing | public category gate |

**DoD:** FalseCategory(p) ∧ CorrectKind(p) for all p. **First-name the false category, refute it, then declare the new kind.**

---

## 6. Operator State

Day 3 is one operating day, not one repo. The operator state is named because the day ends when the ground is named *enough to stop* — the rest gate is part of the law (Genesis Day 3 closes; the seventh day rests).

| Load | Named state |
|---|---|
| PhysicalLoad | active (running with the dog; bodily rhythm maintained) |
| ExecutiveLoad | high (agent supervision + multi-document court closure + decisions) |
| ConsequenceLoad | institutional (enrollment / degree-path commitments with real downstream effect) |
| IdentityTransition | in motion (naming a new public category; moving from old categories to new kinds) |
| RestGate | **named and admissible** — the ground is named enough to stop |

**DoD:** Operator state is named (PhysicalLoad, ExecutiveLoad, ConsequenceLoad, IdentityTransition, RestGate).

This matters: a Day 3 that names every code kind but hides the operator's load has not surveyed the whole ground. The rest gate is reached when naming is sufficient — not when all work is done.

---

## Final Constellation DoD

```
DAY3_DONE means:

1. Scripture frame aligned:
   Genesis Day 3 = ground appears + kinds named + seed-bearing fruit.
   Nehemiah Day 3 = boundary surveyed before rebuild.

2. Whole constellation named:
   ggen, wasm4pm, wasm4pm-compat, cargo-cicd, cargo-cicd-lsp, Spec Kit,
   CONSTRUCT8, Blue River Dam/Truex, PCC, ZOE/Nehemiah, LinkedIn.

3. Each project has: Kind, Provider, Judge, Proof, Witness, Branches,
   FalseOldCategory, CorrectNewCategory, ResearchQuestion, SeedNextSurface.

4. ggen ranked as the biggest paradigm shift (provision after kind).

5. process intelligence ranked second (judgment of provision→consequence).

6. Cross-project dependency graph exists. No floating project.

7. Public-language category map exists (false → correct per project).

8. Branch discipline applies across all projects, not only code.

9. Operator state named (physical, executive, consequence, identity, rest).

10. No implementation hidden inside research.
```

**Verdict: `DAY3_CONSTELLATION_RESEARCH_READY`**

The code cell (compat → ggen → wasm4pm) is kind-closed (`DAY3_KIND_CLOSURE_READY`, `DAY3_KIND_LEDGER.md`). The full constellation is surveyed and named after kind. Every active ground has its kind, role, false→correct category, research question, and place in the dependency graph. The operator state is named, and the rest gate is admissible.

---

## The Missing Sentence

> **Day 3 is not "finish ggen research." Day 3 is "make dry land appear across the whole constellation." ggen is the largest new kind — but the day is done only when every active ground is named after kind.**

*This atlas is the constellation-level Day 3 record. The per-cell kind closure (`DAY3_KIND_LEDGER.md`) is one ground within it. No project's next seed is admissible until that project's kind, boundary, and branches are named here.*
