# Paper Coverage Ledger

**Repository:** wasm4pm-compat  
**Corpus:** ~/Documents/Papers/workflow (20 papers) + ~/Documents/Papers/AI_LLM (1 PM paper) + ~/Documents/Papers/RDF (3 scanned, 0 PM)  
**Generated:** 2026-05-30 | **Updated:** 2026-05-31 (graduation boundary targets named; #33–#38 foundational papers added; all six graduation boundaries and type-law surfaces anchored; #45–#50 added: Heuristics Miner, Weske BPM, BPMN 2.0 OMG, Multi-Perspective PM, OC-PM Divergence/Convergence, DFG Mining & Filtering; #51–#56 added: Process Querying Methods, Event Abstraction, Concept Drift Detection, Predictive Process Monitoring survey, MagTempMiner, Causal Nets; #57–#61 added: Stochastic Conformance, OCEL Extraction/Materialization, Petri Net Unfoldings, Mining Roles, Process Performance Analysis)  
**Agent:** Paper Discovery

---

## Classification Key

| Canon Family | Meaning |
|---|---|
| `OCEL_OBJECT_CENTRIC` | OCEL 2.0 / object-centric event log format |
| `XES_EVENT_LOG` | XES standard / classic flat event log |
| `PETRI_NETS` | Petri net foundations |
| `WF_NET_SOUNDNESS` | Workflow-net soundness (WF-net, free-choice, etc.) |
| `OBJECT_CENTRIC_PETRI` | OC-Petri nets |
| `PROCESS_TREES_INDUCTIVE` | Process trees, inductive miner |
| `POWL` | Partially Ordered Workflow Language |
| `DECLARE_CONSTRAINTS` | Declare / LTL constraint models |
| `LOG_SKELETON` | Log skeleton conformance |
| `DFG_OBJECT_CENTRIC` | Directly-Follows Graphs (OC-DFG) |
| `CONFORMANCE_ALIGNMENT` | Alignment-based / token-replay conformance |
| `PREDICTION_DRIFT` | Predictive monitoring / concept drift |
| `OCPQ_QUERYING` | Object-centric process querying |
| `WORKFLOW_PATTERNS_BPMN` | Workflow patterns / BPMN notation |
| `SYSTEMS_API` | Software library / system / tool paper |
| `OUT_OF_SCOPE` | Not process-mining or type-law relevant |

| Verdict | Meaning |
|---|---|
| `COVERED_BY_TYPE` | Core formal objects already reified as Rust types |
| `COVERED_BY_FIXTURE` | Covered by compile-pass/fail UI fixtures |
| `COVERED_BY_GRADUATION_BOUNDARY` | Structure defined; engine logic must graduate to wasm4pm |
| `DUPLICATE_OR_BACKGROUND` | Redundant with another paper; used as background only |
| `OUT_OF_SCOPE_WITH_REASON` | No process-mining type-law relevance |
| `PARTIAL_WITH_REASON` | Partially covered; specific gaps noted |
| `MISSING_TYPE_LAW` | Canon family present but no Rust type surface yet |

---

## Full Inventory Table

| # | Path | Title | Year | Authors | Canon Family | Key Formal Objects | Paper Law | Current Rust Surface | Zero-cost Status | Pass Fixture | Fail Fixture | .stderr | Missing Work | Verdict |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | `Compliance-Aware Predictive Process Monitoring- A Neuro-Symbolic Approach .pdf` | Compliance-Aware Predictive Process Monitoring: A Neuro-Symbolic Approach | 2026 | De Santis, Park, van der Aalst, Zanichelli | `PREDICTION_DRIFT` | LTN knowledge base, process constraints, prefix-based prediction, compliance score | Predictive model must respect domain constraints expressed as first-order logic; compliance is a typed property of a prefix, not a free label | `src/prediction.rs` defines `PredictionTarget` structure only; no compliance-constraint binding type | Zero-cost possible: compliance constraint as `PhantomData` witness on prediction target | None | None | None | Add `ComplianceConstraintWitness<W>` binding prediction surface to a named law; `Between01` metric for compliance score | `PARTIAL_WITH_REASON` — prediction surface exists but compliance-as-law binding is missing |
| 2 | `HANDSON_PYTHON_FOR_DEVOPS.pdf` | Hands-On Python for DevOps | 2024 | Ankur Roy | `OUT_OF_SCOPE` | None | None | None | N/A | None | None | None | None | `OUT_OF_SCOPE_WITH_REASON` — Python DevOps book; no process-mining or type-law relevance |
| 3 | `Hierarchical Decomposition of Separable Workflow-Nets .pdf` | Hierarchical Decomposition of Separable Workflow-Nets | 2026 | Kourani, Park, van der Aalst | `POWL` | WF-net, separable WF-net, POWL 2.0, choice graph, partial order, state machine subclass, marked graph subclass | WF-net → POWL 2.0 transformation preserves language; separability is a structural law; choice graphs cover non-block-structured decision and cyclic logic | `src/powl.rs` has `TreeProjectable` sealed, `ChoiceGraphEdge`, `PowlNodeKind::ChoiceGraph`, `WfNet2PowlWitness`; `src/petri.rs` has `SeparableWfNet<SOUNDNESS>` | Zero-cost: full surface — `SeparableWfNet`, `ChoiceGraph`, `WfNet2PowlWitness` all added | `tests/ui/compile_pass/separable_wfnet_marker.rs`, `tests/ui/compile_pass/wfnet2powl_witness.rs` | None | None | Compile-fail fixture for forged non-separable conversion still missing | `COVERED_BY_TYPE` — all key formal objects (SeparableWfNet, ChoiceGraph, WfNet2PowlWitness) reified; conversion witness graduation path defined |
| 4 | `How-Anthropic-teams-use-Claude-Code_v2.pdf` | How Anthropic Teams Use Claude Code | 2024 | Anthropic | `OUT_OF_SCOPE` | None | None | None | N/A | None | None | None | None | `OUT_OF_SCOPE_WITH_REASON` — internal Anthropic tool usage document; no process-mining relevance |
| 5 | `Object-Centric Analysis of XES Event Logs- Integrating OCED Modeling with SPARQL Queries .pdf` | Object-Centric Analysis of XES Event Logs: Integrating OCED Modeling with SPARQL Queries | 2025 | Latif, Latif, Rahman | `XES_EVENT_LOG` | XES event log, OCED meta-model, SPARQL query, event-object relationship, object relation type | XES-to-OCED conversion must preserve event-object linkage without data loss; OCED adds qualifiers and object-to-object relations absent in XES | `src/xes.rs` and `src/ocel.rs` define structure; `src/interop.rs` bridges formats | Zero-cost: loss surfaces via `LossReport<Xes, Oced, Items>` | None | None | None | Explicit `XesToOcedProjection` with `LossPolicy::AllowLossWithReport`; SPARQL-queryable object graph shape | `PARTIAL_WITH_REASON` — XES and OCEL structures exist; XES→OCED interop projection path needs explicit loss surface |
| 6 | `OCPQ- Object-Centric Process Querying & Constraints .pdf` | OCPQ: Object-Centric Process Querying & Constraints | 2025 | Küsters, van der Aalst | `OCPQ_QUERYING` | OCED tuple `(E, O, eaval, oaval)`, event/object universes, nested query, constraint violation set, object variable, event variable | Queries are typed over OCED; constraint checking returns typed violation sets per object type, not free strings; nested queries span multiple object and event types | `src/ocpq.rs` defines `OcpqQuery` and `OcpqResult` structure with predicate witness markers (`EventPredicate`, `ObjectPredicate`, `RelationPredicate`, `TemporalPredicate`, `CardinalityPredicate`, `NestedQuery`, `Constraint`) and `OcpqRefusal` surface | Zero-cost: `OcpqQuery<ObjectTypes, EventTypes>` with const-generic type sets | None | None | None | Typed `ObjectTypeSet` and `EventTypeSet` const params; `ConstraintViolation<ObjType, EvType>` typed result; compile-fail for mixing object types across constraint scopes | `PARTIAL_WITH_REASON` — OCED query structure and refusal surface implemented in `src/ocpq.rs`; full const-generic `ObjectTypeSet`/`EventTypeSet` type params not yet surface-level |
| 7 | `PM4Py Software Impacts.pdf` | PM4Py: A process mining library for Python (Software Impacts edition) | 2023 | Berti, van Zelst, Schuster | `SYSTEMS_API` | OC-DFG, OC-Petri net, XES, OCEL, PTML, PNML, BPMN 2.0, token-based replay, alignments, log skeleton, soundness check | PM4Py surface defines the canonical algorithm vocabulary this crate must structure-encode; soundness check (`check_soundness`) maps to `WfNetConst<SOUNDNESS>` | `src/` modules encode PM4Py's structural objects as zero-cost types | Zero-cost by design: this crate is the typed structure layer that PM4Py's runtime consumes | `tests/ui/compile_pass/` (planned) | `tests/ui/compile_fail/` (planned) | planned | Ensure every PM4Py-named format/algorithm has a named Rust type surface; doctest for each | `COVERED_BY_GRADUATION_BOUNDARY` — **Graduates:** token-based replay (`GraduationReason::NeedsReplay`), alignment-based conformance checking (`NeedsConformanceExecution`), log skeleton conformance (`NeedsConformanceExecution`), process discovery algorithms — Alpha, Inductive Miner, Heuristics Miner (`NeedsDiscovery`), soundness checking execution (`NeedsConformanceExecution`). Structural canon (OC-DFG, OC-Petri net, OCEL, XES, BPMN 2.0 shapes) remains in this crate. |
| 8 | `PM4Py- A process mining library for Python.pdf` | PM4Py: A process mining library for Python | 2023 | Berti, van Zelst, Schuster | `SYSTEMS_API` | Same as #7 — identical paper (Software Impacts journal version) | Same as #7 | Same as #7 | Same as #7 | Same as #7 | Same as #7 | Same as #7 | Same as #7 | `DUPLICATE_OR_BACKGROUND` — same paper as #7, duplicate in corpus |
| 9 | `PMAx- An Agentic Framework for AI-Driven Process Mining .pdf` | PMAx: An Agentic Framework for AI-Driven Process Mining | 2026 | Antonov, Kourani, Berti, Park, van der Aalst | `SYSTEMS_API` | Engineer agent, Analyst agent, schema abstraction, static verification layer, local execution, PM4Py artifact | Agentic framework separates structural schema extraction from execution; schema layer is exactly the structural surface this crate provides | `src/` modules provide the structural schema PM4Py/PMAx agents consume | Zero-cost: no runtime in this crate; PMAx runtime graduation boundary | None | None | None | Ensure `EventLog` schema extraction surface is zero-cost typed (no execution logic) | `COVERED_BY_GRADUATION_BOUNDARY` — **Graduates:** Engineer agent local PM4Py execution (`NeedsDiscovery`, `NeedsConformanceExecution`), Analyst agent report/insight generation (`RebuildingProcessMiningLocally`), PM4Py artifact materialization (discovery results, alignment matrices, DFG computation). Schema abstraction layer (event log shape, Petri net shape, OCEL shape) remains in this crate as `GraduationCandidate`-marked types. |
| 10 | `Process mining for healthcare- Characteristics and challenges .pdf` | Process mining for healthcare: Characteristics and challenges | 2022 | Munoz-Gama, Martin, Fernandez-Llatas, et al. | `OUT_OF_SCOPE` | Event log, case id, activity, timestamp, transaction type, resource | Healthcare domain characteristics for process mining application; no new formal objects beyond standard PM | Standard PM structures already in canon | N/A — domain application paper | None | None | None | None | `OUT_OF_SCOPE_WITH_REASON` — domain application survey; all formal objects already in canon; no new type-law surface required |
| 11 | `Real-Life BPMN - edition 4.pdf` | Real-Life BPMN (4th edition) | 2019 | Freund, Rücker | `WORKFLOW_PATTERNS_BPMN` | BPMN 2.0 elements (task, gateway, event, pool, lane, DMN), XOR/AND/OR-gateway, subprocess, message flow | BPMN notation defines the structural vocabulary for process model import/export; gateway type is a named structural law | `src/bpmn.rs` defines BPMN structure | Zero-cost: gateway type as `PhantomData` witness; pool/lane as struct fields | None | None | None | Typed gateway enum matching BPMN 2.0 spec; compile-fail for invalid gateway semantics | `COVERED_BY_TYPE` — BPMN structures are in `src/bpmn.rs`; practical BPMN examples confirm coverage |
| 12 | `sAirflow- Adopting Serverless in a Legacy Workflow Scheduler .pdf` | sAirflow: Adopting Serverless in a Legacy Workflow Scheduler | 2024 | Mikina, Zuk, Rzadca | `OUT_OF_SCOPE` | DAG workflow, FaaS, CDC, task instance, serverless executor | Serverless DAG scheduling; no process-mining formal objects; no event log, no conformance, no process model canon | None | N/A | None | None | None | None | `OUT_OF_SCOPE_WITH_REASON` — infrastructure/systems paper about Airflow serverless migration; no process-mining type-law relevance |
| 13 | `Why Automate This? Exploring the Connection between Time Use, Well-being and Robot Automation Across Social Groups.pdf` | Why Automate This? Exploring the Connection between Time Use, Well-being and Robot Automation Across Social Groups | 2025 | Ray, Pang, Srivastava, Fei-Fei, Shorey, Martín-Martín | `OUT_OF_SCOPE` | None | None | None | N/A | None | None | None | None | `OUT_OF_SCOPE_WITH_REASON` — HRI/sociology paper on household robot automation preferences; entirely outside process mining |
| 14 | `workflow-patterns-the-definitive-guide-9780262029827-9780262329408-0262329409_compress.pdf` | Workflow Patterns: The Definitive Guide | 2016 | Russell, van der Aalst, ter Hofstede | `WORKFLOW_PATTERNS_BPMN` | Control-flow patterns (sequence, parallel split, synchronization, exclusive choice, simple merge, multi-choice, synchronizing merge, discriminator, arbitrary cycle, milestone), data patterns, resource patterns, exception patterns | Each workflow pattern is a named structural law; pattern coverage is a formal correctness property of a workflow language | `src/law.rs` has `WorkflowPattern` as `ConstParamTy` enum covering 17 of 20 canonical patterns; `src/bpmn.rs`, `src/petri.rs`, `src/powl.rs` collectively encode pattern-reachable structures | Zero-cost: `WorkflowPattern` as `ConstParamTy` const-generic param; a `PatternNet<{ WorkflowPattern::ParallelSplit }>` is a distinct type from `PatternNet<{ WorkflowPattern::ExclusiveChoice }>` | `tests/ui/compile_pass/workflow_pattern_const_param.rs` | `tests/ui/compile_fail/workflow_pattern_wrong_kind.rs` | `tests/ui/compile_fail/workflow_pattern_wrong_kind.stderr` | 3 remaining patterns (WCP-14, WCP-15, WCP-18) not yet named; data/resource/exception patterns out of scope | `COVERED_BY_TYPE` — 17 control-flow patterns reified as `ConstParamTy` variants with compile-fail seal |
| 15 | `Workflows Community Summit 2024- Future Trends and Challenges in Scientific Workflows  .pdf` | Workflows Community Summit 2024: Future Trends and Challenges in Scientific Workflows | 2024 | Ferreira da Silva et al. (111 participants) | `OUT_OF_SCOPE` | Scientific workflow, HPC, FAIR workflow, AI-HPC convergence, multi-facility | Scientific/HPC workflow community report; no process-mining formal objects; different workflow paradigm (data-flow/HPC vs. business process) | None | N/A | None | None | None | None | `OUT_OF_SCOPE_WITH_REASON` — scientific/HPC workflows community report; different domain from BPM/process mining |
| 16 | `YAWL - Technical Manual.pdf` | YAWL Technical Manual (Version 5) | 2023 | The YAWL Foundation | `SYSTEMS_API` | YAWL engine, custom service API, worklet service, task decomposition, cancellation region, YAWL specification (XSD), Interface B observer gateway | YAWL specification format defines workflow net structures; engine interfaces define the execution boundary that must not cross into this crate | `src/interop.rs` may reference YAWL-compatible net structure | Zero-cost: YAWL spec as import-only surface; no engine logic | None | None | None | `YawlSpecWitness` marker for YAWL-origin nets; import surface in `src/formats.rs` | `COVERED_BY_GRADUATION_BOUNDARY` — **Graduates:** YAWL engine task execution and worklet service dispatch (`RebuildingProcessMiningLocally`), Interface B observer gateway (runtime conformance monitoring, `NeedsConformanceExecution`), custom service API invocation (`NeedsReceipts` for provenance chain). Structural surface that remains: YAWL net import shape, cancellation region type, task decomposition shape — these are `GraduationCandidate`-marked structure in this crate. |
| 17 | `YAWL_An_open_source_Business_Process_Management_Sy.pdf` | YAWL: An open source Business Process Management System from science for science | 2020 | Adams, Hense, ter Hofstede | `SYSTEMS_API` | YAWL process model, YAWL engine, worklet, cancellation set, XES logging, resource service | YAWL generates XES-format logs; control flow is WF-net based; soundness is checked via WOFLAN | `src/petri.rs`, `src/xes.rs` cover the structural objects | Zero-cost: YAWL→XES log as `LossyFormatExport` | None | None | None | Explicit `YawlToXes` projection with loss accounting | `DUPLICATE_OR_BACKGROUND` — overlaps with YAWL Technical Manual (#16) and YAWL language paper (#18); background only |
| 18 | `YAWL- Yet Another Workflow Language.pdf` | YAWL: Yet Another Workflow Language (Revised version) | 2004 | van der Aalst, ter Hofstede | `WF_NET_SOUNDNESS` | WF-net, workflow patterns (20), AND/OR/XOR-split/join, cancellation regions, multiple instances, YAWL net, soundness via reduction | WF-net soundness is a first-class structural law; YAWL extends Petri nets precisely to cover the 20 workflow patterns; soundness can be verified compositionally | `src/petri.rs` has `WfNetConst<SOUNDNESS>` with non-forgeable witness path | Zero-cost: `WfNetConst<true>` vs `WfNetConst<false>` const-generic soundness tag | `tests/ui/compile_pass/` — lawful WF-net construction | `tests/ui/compile_fail/` — forging soundness witness | `.stderr` — expected "cannot construct WfNetSoundnessWitness" | Named cancellation region type; multiple-instance construct type | `COVERED_BY_TYPE` — WF-net soundness is the primary law in `src/petri.rs` |
| 19 | `YAWLTechnicalManual5.0.pdf` | YAWL Technical Manual Version 5 | 2023 | The YAWL Foundation | `SYSTEMS_API` | Same as #16 — YAWL engine API, custom service, worklet service | Same as #16 | Same as #16 | Same as #16 | Same as #16 | Same as #16 | Same as #16 | Same as #16 | `DUPLICATE_OR_BACKGROUND` — same document as #16 (both are YAWL Technical Manual v5) |
| 20 | `YAWLUserManual5.1.pdf` | YAWL User Manual Version 5.1 | 2024 | The YAWL Foundation | `SYSTEMS_API` | YAWL editor, workflow specification, control-flow perspective, data perspective, resource perspective, cancellation set, task timer | User-facing YAWL manual; no new formal laws beyond YAWL language paper (#18) | Same as #16/#19 | N/A — user manual | None | None | None | None | `OUT_OF_SCOPE_WITH_REASON` — end-user manual; no new formal objects or type-law surfaces; background reference only |
| 21 | `AI_LLM/No AI Without PI! Object-Centric Process Mining as the Enabler for Generative, Predictive, and Prescriptive Artificial Intelligence .pdf` | No AI Without PI! Object-Centric Process Mining as the Enabler for Generative, Predictive, and Prescriptive Artificial Intelligence | 2025 | van der Aalst | `OBJECT_CENTRIC_PETRI` | OCED/OCPM pipeline (discovery, compliance analysis, performance analysis), process intelligence (PI) layer, generative/predictive/prescriptive AI boundary | Process intelligence requires OCED-grounded data; discovery, compliance, performance are typed operations over OCED; AI outputs must trace back to lawful process evidence | `src/ocel.rs` (OCED structure), `src/conformance.rs` (compliance), `src/prediction.rs` (predictive AI boundary), `src/dfg.rs` (performance) | Zero-cost: all OCED structural surfaces already defined; AI engine logic must graduate | None | None | None | None — all structural surfaces already in canon; AI integration is engine graduation boundary | `COVERED_BY_GRADUATION_BOUNDARY` — **Graduates:** OCED process discovery execution (Alpha/Inductive Miner over OCED, `NeedsDiscovery`), compliance analysis execution (token replay/alignment over OCED, `NeedsConformanceExecution`), performance analysis computation (DFG frequency/time aggregation, `NeedsObjectCentricQueryExecution`), generative AI output grounding (`NeedsReceipts`), predictive model inference (`RebuildingProcessMiningLocally`). OCED structural surfaces (`OcelLog`, `OcelEvent`, `OcelObject`, `ConformanceMetric`, `PredictionTarget`, `Dfg`) remain in this crate as the typed evidence base. |
| 22 | `RDF/Ontology-Driven Model-to-Model Transformation of Workflow Specifications .pdf` | Ontology-Driven Model-to-Model Transformation of Workflow Specifications | 2025 | Abreu, Cruz, Guerreiro | `OUT_OF_SCOPE` | RML semantic lifting, OWL ontology alignment, BPMN 2.0 generation via Camunda API, M2M transformation pipeline | None | `src/bpmn.rs` already covers BPMN 2.0 target structures; RML/OWL toolchain is import pipeline concern | N/A | None | None | None | None | `OUT_OF_SCOPE_WITH_REASON` — M2M transformation toolchain paper; BPMN 2.0 target structures already in `src/bpmn.rs`; semantic lifting pipeline imposes no new type-law surface |
| 23 | `RDF/Procedure Model for Building Knowledge Graphs for Industry Applications .pdf` | Procedure Model for Building Knowledge Graphs for Industry Applications | 2024 | Meckler | `OUT_OF_SCOPE` | KG construction lifecycle (CRISP-DM derived), RDF/OWL/SPARQL stack, competency questions, ontology modeling | None | None | N/A | None | None | None | None | `OUT_OF_SCOPE_WITH_REASON` — KG construction methodology; no process-mining formal objects (no event logs, Petri nets, process trees, or conformance surfaces) |
| 24 | `RDF/RDFGraphGen- An RDF Graph Generator based on SHACL Shapes .pdf` | RDFGraphGen: An RDF Graph Generator based on SHACL Shapes | 2025 | Jovanovik, Vecovska, Jakubowski, Hose | `OUT_OF_SCOPE` | SHACL shapes as generation blueprints, synthetic RDF graph generation, scale-factor parameterization | None | None | N/A | None | None | None | None | `OUT_OF_SCOPE_WITH_REASON` — SHACL-based RDF data generation tool; no process-mining relevance |
| 25 | `OCEL-2.0-specification.pdf` | OCEL 2.0 Specification | 2023 | van der Aalst, Berti, Ghahfarokhi, Klijn, Park, Pourbafrani | `OCEL_OBJECT_CENTRIC` | OcelLog, OcelEvent, OcelObject, EventObjectLink, ObjectObjectLink, OcelDims, OcelAttribute | OCEL 2.0 formal data model defines object-centric event log structure; event-to-object and object-to-object links are first-class structural elements, not optional annotations | `src/ocel.rs` — `OcelLog`, `OcelEvent`, `OcelObject`, `EventObjectLink`, `ObjectObjectLink`, `OcelDims`, `OcelAttribute` directly implement this spec's formal data model | Zero-cost: all OCEL 2.0 formal shapes reified as Rust structs/newtypes; no runtime in compat | `compile_pass/ocel_event_object_relation.rs`, `compile_pass/ocel_object_object_relation.rs` | `compile_fail/ocel_e2o_missing_link.rs`, `compile_fail/ocel_o2o_missing_link.rs` | `ocel_e2o_missing_link.stderr`, `ocel_o2o_missing_link.stderr` | None — all key formal objects from the spec are reified | `COVERED_BY_TYPE` — OcelLog/Event/Object/EventObjectLink/ObjectObjectLink/OcelDims/OcelAttribute directly implement OCEL 2.0 spec formal data model |
| 26 | `xes-ieee-1849-2023.pdf` | XES IEEE Standard 1849-2023 | 2023 | IEEE Standards Association | `XES_EVENT_LOG` | XesLog, XesTrace, XesEvent, CaseCentricMarker, XesExtension | XES standard defines the formal schema for classic flat event logs; case-centric structure and extension declarations are first-class structural laws, not implementation choices. Formal IEEE standard, not a discovery algorithm | `src/xes.rs` — `XesLog`, `XesTrace`, `XesEvent`, `CaseCentricMarker`, `XesExtension`, `XesRefusal::UndeclaredExtensionPrefix` implement the XES standard shapes | Zero-cost: all XES standard shapes reified; `CaseCentricMarker` seals the flat vs. object-centric distinction at the type level | `compile_pass/xes_case_centric_log.rs`, `compile_pass/xes_declared_extension_prefix.rs` | `compile_fail/xes_not_object_centric.rs`, `compile_fail/xes_undeclared_extension_prefix_rejected.rs` | `xes_not_object_centric.stderr`, `xes_undeclared_extension_prefix_rejected.stderr` | None — all key standard shapes are reified | `COVERED_BY_TYPE` — XesLog/Trace/Event/CaseCentricMarker/XesExtension implement XES IEEE 1849-2023 standard shapes |
| 27 | `inductive-miner-2013.pdf` | Inductive Miner: Discovering Block-Structured Process Models | 2013 | Leemans, Fahland, van der Aalst | `PROCESS_TREES_INDUCTIVE` | ProcessTree, TypedLoopNode, ProcessOperator (sequence, exclusive choice, parallel, loop), directly-follows relation, inductive mining cut | Inductive Miner produces block-structured process trees; tree node types (sequence, choice, parallel, loop) are structural laws; loop node arity must be exactly 2 (body + redo). Discovery algorithm execution graduates to wasm4pm | `src/process_tree.rs` — `ProcessTree`, `TypedLoopNode<ARITY>` with `Require<{ ARITY == 2 }>: IsTrue`, `ProcessOperator`; `src/powl.rs` — `TreeProjectable` sealed trait | Zero-cost: `TypedLoopNode<ARITY>` const-generic arity check; `ProcessOperator` as `ConstParamTy` enum variant | `compile_pass/process_tree_loop_arity_2.rs` | `compile_fail/process_tree_bad_loop_arity.rs` | `process_tree_bad_loop_arity.stderr` | None for structure; inductive mining execution must graduate | `COVERED_BY_GRADUATION_BOUNDARY` — process tree SHAPES (ProcessTree/TypedLoopNode/ProcessOperator) typed in compat; inductive mining execution graduates to wasm4pm |
| 28 | `declare-ltl-2006.pdf` | Declare: Full Support for Loosely-Structured Processes | 2006 | Pesic, van der Aalst | `DECLARE_CONSTRAINTS` | DeclareConstraint, DeclareTemplate, DeclareWitness, constraint activation/correlation conditions, binary relation between activities | Declare constraint model defines named templates (existence, absence, response, precedence, etc.) as first-class structural laws; each template is a distinct type, not a free string. Constraint CHECKING execution graduates to wasm4pm | `src/declare.rs` — `DeclareConstraint`, `DeclareTemplate`, `DeclareWitness` implement the Declare constraint shapes; `declare_binary_arity_rejected` law enforces arity ≥ 2 | Zero-cost: `DeclareTemplate` as `ConstParamTy` variant; `DeclareWitness` as `PhantomData` proof that constraint came from a named template | `compile_pass/declare_constraint_shape.rs` | `compile_fail/declare_binary_arity_rejected.rs` | `declare_binary_arity_rejected.stderr` | None for structure; constraint checking execution must graduate | `COVERED_BY_TYPE` — DeclareConstraint/Template/Witness implement Declare constraint shapes; binary arity law enforced by compile-fail fixture |
| 29 | `alpha-miner-2004.pdf` | Workflow Mining: Discovering Process Models from Event Logs | 2004 | van der Aalst, Weijters, Maruster | `PETRI_NETS` | PlaceToTransitionArc, TransitionToPlaceArc, WfNetConst, SoundnessState, footprint matrix, directly-follows relation | Alpha Miner discovers Petri nets from event logs via footprint matrix analysis; the output is a WF-net with typed bipartite arcs. Petri net SHAPES (typed arcs, WfNetConst soundness) are the structural compliance surface; mining execution graduates to wasm4pm | `src/petri.rs` — `PlaceToTransitionArc`, `TransitionToPlaceArc`, `WfNetConst<SOUNDNESS>` with non-forgeable witness path; `petri_place_to_place_arc` and `petri_transition_to_transition_arc` laws enforce bipartite structure | Zero-cost: typed arc newtypes enforce bipartite law at compile time; `WfNetConst<true>` vs `WfNetConst<false>` soundness const-generic | `compile_pass/petri_place_to_transition_arc.rs`, `compile_pass/petri_transition_to_place_arc.rs`, `compile_pass/wfnet_with_soundness_witness.rs` | `compile_fail/petri_place_to_place_arc.rs`, `compile_fail/petri_transition_to_transition_arc.rs`, `compile_fail/wfnet_forged_soundness.rs` | `petri_place_to_place_arc.stderr`, `petri_transition_to_transition_arc.stderr`, `wfnet_forged_soundness.stderr` | None for structure; Alpha Miner footprint-matrix computation must graduate | `COVERED_BY_GRADUATION_BOUNDARY` — Petri net SHAPES (typed bipartite arcs, WfNetConst soundness) typed in compat; mining execution graduates to wasm4pm |
| 30 | `log-skeleton-2018.pdf` | Log Skeleton: An Accurate Conformance Checker for Petri Nets | 2018 | Verbeek | `LOG_SKELETON` | DeclareConstraint (always-after, always-before, never-together, equivalence, directly-follows), activity frequency bounds, equivalence classes | Log skeleton encodes declarative constraints derived from logs; each skeleton constraint is a named structural law. DeclareConstraint shapes provide the structural surface; log-driven skeleton computation graduates to wasm4pm | `src/declare.rs` — `DeclareConstraint`, `DeclareTemplate`, `DeclareWitness` provide the constraint shapes that underpin log skeleton encoding | Zero-cost: log skeleton constraints map to `DeclareTemplate` variants; constraint derivation from logs is runtime execution | `compile_pass/declare_constraint_shape.rs` | `compile_fail/declare_binary_arity_rejected.rs` | `declare_binary_arity_rejected.stderr` | None for structure; log skeleton derivation algorithm from event logs must graduate | `COVERED_BY_GRADUATION_BOUNDARY` — DeclareConstraint shapes provide the structural surface; log-driven skeleton derivation execution graduates to wasm4pm |
| 31 | `oc-petri-nets-2019.pdf` | Object-Centric Behavioral Constraints | 2019 | van der Aalst | `OBJECT_CENTRIC_PETRI` | PlaceToTransitionArc, TransitionToPlaceArc, WfNetConst, object-centric arc inscription, variable arc, binding element, execution semantics per object type | OC-Petri nets extend standard Petri nets with object-centric arc types; each arc is inscribed with an object type, enabling multi-instance token semantics. The typed bipartite arc law and WfNetConst soundness are the structural compliance surfaces | `src/petri.rs` — `PlaceToTransitionArc`, `TransitionToPlaceArc`, `WfNetConst<SOUNDNESS>`, `SoundnessState`; typed bipartite arc law enforced by `petri_place_to_place_arc` and `petri_transition_to_transition_arc` compile-fail fixtures | Zero-cost: typed arc newtypes enforce object-centric bipartite law; `WfNetConst` soundness const-generic; arc inscription types as `PhantomData` object-type markers | `compile_pass/petri_place_to_transition_arc.rs`, `compile_pass/wfnet_with_soundness_witness.rs` | `compile_fail/petri_place_to_place_arc.rs`, `compile_fail/petri_transition_to_transition_arc.rs` | `petri_place_to_place_arc.stderr`, `petri_transition_to_transition_arc.stderr` | Object-type arc inscription as typed `PhantomData` markers; variable arc distinction from regular arc | `COVERED_BY_TYPE` — typed bipartite arcs (PlaceToTransitionArc/TransitionToPlaceArc) and WfNetConst soundness are the structural compliance surfaces for OC-Petri nets |
| 32 | `conformance-alignments-2011.pdf` | Aligning Observed and Modeled Behavior | 2011 | Adriansyah, van Dongen, Munoz-Gama, Carmona | `CONFORMANCE_ALIGNMENT` | FitnessConst, PrecisionConst, F1Const, alignment cost function, synchronous product net, move-on-log, move-on-model | Alignment-based conformance checking produces fitness and precision metrics bounded in [0,1]; a metric that escapes the unit interval is a type error. Metric SHAPES (Between01-bounded) are typed in compat; alignment computation graduates to wasm4pm | `src/conformance.rs` — `Metric<KIND, NUM, DEN>` with `Between01<NUM, DEN>` bounds; `metric_out_of_bounds` compile-fail law rejects NUM > DEN; `FitnessConst`, `PrecisionConst`, `F1Const` const-generic metric types | Zero-cost: `Between01<NUM, DEN>` const-generic bound enforced at compile time; `Metric<KIND, NUM, DEN>` distinct types per metric kind | `compile_pass/ocpq_cardinality_valid_bounds.rs` | `compile_fail/metric_out_of_bounds.rs` | `metric_out_of_bounds.stderr` | None for metric shapes; alignment computation (synchronous product net construction, A* cost search) must graduate | `COVERED_BY_GRADUATION_BOUNDARY` — FitnessConst/PrecisionConst/F1Const metric shapes (Between01-bounded) typed in compat; alignment computation graduates to wasm4pm |
| 33 | `murata-petri-nets-1989.pdf` | Petri Nets: Properties, Analysis and Applications | 1989 | Murata | `PETRI_NETS` | Place, Transition, PlaceToTransitionArc, TransitionToPlaceArc, incidence matrix W-/W+, enabling condition, firing rule, marking reachability, liveness, boundedness, safeness | Petri net bipartite arc law (no P→P or T→T arcs) and enabling condition (all input places marked) are first-class structural laws; every lawful net firing is grounded in the Murata (1989) §2 incidence-matrix formulation | `src/petri.rs` — `PlaceToTransitionArc`, `TransitionToPlaceArc`, `WfNetConst`, `IncidenceMatrix` W-/W+, enabling condition; `src/nightly_foundry.rs` — `petri_law` and `token_law` surfaces directly cite Murata (1989) §2 | Zero-cost: typed arc newtypes enforce bipartite law at compile time; `IncidenceMatrix` W-/W+ are structure-only types; enabling condition as const-generic predicate | `compile_pass/petri_place_to_transition_arc.rs`, `compile_pass/petri_transition_to_place_arc.rs` | `compile_fail/petri_place_to_place_arc.rs`, `compile_fail/petri_transition_to_transition_arc.rs` | `petri_place_to_place_arc.stderr`, `petri_transition_to_transition_arc.stderr` | None — core bipartite law and incidence-matrix shapes fully covered | `COVERED_BY_TYPE` — `PlaceToTransitionArc`, `TransitionToPlaceArc`, `WfNetConst`, `IncidenceMatrix` W-/W+, enabling condition reified; `petri_law` and `token_law` in `src/nightly_foundry.rs` directly cite Murata (1989) §2 |
| 34 | `van-der-aalst-wf-net-soundness-1998.pdf` | The Application of Petri Nets to Workflow Management | 1998 | van der Aalst | `WF_NET_SOUNDNESS` | WF-net (source/sink place, initial/final marking), soundness criterion (option completeness, proper completion, no dead transitions), WF-net reduction, WF-net to free-choice net transformation | WF-net soundness is a named first-class structural law: option completeness (every reachable state can reach the final marking), proper completion (final marking is the unique terminal state), no dead transitions (every transition is reachable). The `WfNetSoundnessPaper` witness in `src/witness.rs` is the non-forgeable receipt for this criterion | `src/petri.rs` — `WfNetConst<SOUNDNESS>`, `WfNetSoundnessWitness` non-forgeable path (constructor is `pub(crate)`); `src/witness.rs` — `WfNetSoundnessPaper` witness marker | Zero-cost: `WfNetConst<true>` vs `WfNetConst<false>` const-generic soundness tag; `WfNetSoundnessWitness` non-forgeable by construction | `compile_pass/wfnet_with_soundness_witness.rs` | `compile_fail/wfnet_forged_soundness.rs` | `wfnet_forged_soundness.stderr` | None — soundness criterion fully reified; reduction algorithm graduates to wasm4pm | `COVERED_BY_TYPE` — `WfNetConst<SOUNDNESS>`, non-forgeable `WfNetSoundnessWitness`, `WfNetSoundnessPaper` witness marker implement the WF-net soundness criterion (option completeness, proper completion, no dead transitions) |
| 35 | `ocel-1.0-specification.pdf` | OCEL: A Standard for Object-Centric Event Logs | 2020 | van der Aalst, Berti | `OCEL_OBJECT_CENTRIC` | OcelLog, OcelEvent, OcelObject, EventObjectLink (event-to-object), object type, event type, timestamp ordering | OCEL 1.0 defines the original object-centric event log standard; the event-to-object link structure is the structural ancestor of OCEL 2.0 (which adds object-to-object links and richer attribute model). `Ocel20` witness in `src/witness.rs` covers both versions; OCEL 1.0 event-to-object link shape is a structural subset of OCEL 2.0 | `src/ocel.rs` — `OcelLog`, `OcelEvent`, `OcelObject`, `EventObjectLink`; `src/witness.rs` — `Ocel20` witness covers OCEL 1.0 and 2.0 structural surfaces (OCEL 1.0 predates OCEL 2.0 spec, #25 in ledger) | Zero-cost: same Rust surfaces as OCEL 2.0 (#25); OCEL 1.0 is a structural subset | `compile_pass/ocel_event_object_relation.rs` | `compile_fail/ocel_e2o_missing_link.rs` | `ocel_e2o_missing_link.stderr` | None — OCEL 1.0 structures are a subset of the OCEL 2.0 surfaces already reified | `COVERED_BY_TYPE` — OCEL 1.0 event-to-object link structure is the structural ancestor of OCEL 2.0; `OcelLog`/`OcelEvent`/`OcelObject`/`EventObjectLink` in `src/ocel.rs` cover both versions; `Ocel20` witness in `src/witness.rs` subsumes OCEL 1.0 |
| 36 | `discovering-ocpn-2020.pdf` | Discovering Object-Centric Petri Nets | 2020 | van der Aalst | `OBJECT_CENTRIC_PETRI` | PlaceToTransitionArc, TransitionToPlaceArc, arc inscription (object-type PhantomData marker), variable arc vs. regular arc, binding element, OC-Petri net discovery from OCEL | OC-Petri net arc inscriptions (object-type markers) are structural laws that must be typed, not stringly-typed; variable vs. regular arc distinction is a named structural property. The OC-Petri net discovery algorithm (mining from OCEL logs) graduates to wasm4pm | `src/petri.rs` — `PlaceToTransitionArc`, `TransitionToPlaceArc` with object-type inscription PhantomData markers; `src/witness.rs` — `ObjectCentricPetriNetPaper` witness marker | Zero-cost possible: arc inscription as `PhantomData<ObjectType>` const-generic tag; variable arc as distinct newtype from regular arc | `compile_pass/petri_place_to_transition_arc.rs`, `compile_pass/wfnet_with_soundness_witness.rs` | `compile_fail/petri_place_to_place_arc.rs`, `compile_fail/petri_transition_to_transition_arc.rs` | `petri_place_to_place_arc.stderr`, `petri_transition_to_transition_arc.stderr` | Arc inscription as typed `PhantomData<ObjectType>` marker; variable arc as distinct newtype | `COVERED_BY_GRADUATION_BOUNDARY` — typed bipartite arc shapes (PlaceToTransitionArc/TransitionToPlaceArc) and `ObjectCentricPetriNetPaper` witness in `src/witness.rs` are the structural surface; arc inscription PhantomData markers and variable-arc newtype needed; OC-Petri net discovery execution graduates to wasm4pm (`NeedsDiscovery`) |
| 37 | `process-mining-book-2011.pdf` | Process Mining: Discovery, Conformance and Enhancement of Business Processes | 2011 | van der Aalst | `PETRI_NETS` | EventLog, PetriNet, ProcessTree, DeclareConstraint, ConformanceMetric, Dfg, XesLog — all canonical process-mining structural shapes; alpha algorithm, token replay, fitness/precision/simplicity/generalization | This is the foundational PM textbook (van der Aalst 2011/2016) from which all canonical structural shapes in this crate derive; every module in the base profile traces its type-law lineage to this book | All canonical modules: `eventlog`, `petri`, `process_tree`, `declare`, `conformance`, `dfg`, `xes` — all structural shapes are covered; discovery and conformance execution graduates to wasm4pm | Zero-cost: all structural shapes already reified across the canon modules; no new surface required | Multiple compile-pass fixtures across all canon modules | Multiple compile-fail fixtures across all canon modules | Multiple `.stderr` files | None for structural shapes; discovery and conformance execution (alpha algorithm, token replay, alignment) must graduate | `COVERED_BY_GRADUATION_BOUNDARY` — all canonical structural shapes (EventLog, PetriNet, ProcessTree, DeclareConstraint, ConformanceMetric, Dfg, XesLog) are fully reified across the canon modules; discovery algorithms (alpha, inductive miner, heuristics miner) graduate to wasm4pm (`NeedsDiscovery`); conformance execution (token replay, alignment) graduates to wasm4pm (`NeedsConformanceExecution`) |
| 38 | `conformance-checking-book-2018.pdf` | Conformance Checking: Relating Processes and Models | 2018 | Carmona, van Dongen, Solti, Weidlich | `CONFORMANCE_ALIGNMENT` | Metric<KIND,NUM,DEN>, FitnessConst, PrecisionConst, F1Const, Between01<NUM,DEN> bounds, synchronous product net, alignment cost function, A* cost search, move-on-log, move-on-model | Conformance checking produces fitness and precision metrics that are formally bounded in [0,1]; a metric escaping the unit interval is a type error, not a runtime exception. Metric shapes (Between01-bounded) are typed in this crate; alignment computation graduates to wasm4pm. Carmona et al. 2018 is the canonical conformance reference | `src/conformance.rs` — `Metric<KIND, NUM, DEN>` with `Between01<NUM, DEN>` bounds; `FitnessConst`, `PrecisionConst`, `F1Const` const-generic metric types; `metric_out_of_bounds` compile-fail law rejects NUM > DEN | Zero-cost: `Between01<NUM, DEN>` const-generic bound enforced at compile time; `Metric<KIND, NUM, DEN>` distinct types per metric kind | `compile_pass/ocpq_cardinality_valid_bounds.rs` | `compile_fail/metric_out_of_bounds.rs` | `metric_out_of_bounds.stderr` | None for metric shapes; alignment computation (synchronous product net construction, A* cost search) must graduate | `COVERED_BY_GRADUATION_BOUNDARY` — `Metric<KIND,NUM,DEN>` with `Between01` bounds, `FitnessConst`/`PrecisionConst`/`F1Const` metric shapes typed in compat; alignment computation (synchronous product net, A* cost search) graduates to wasm4pm (`NeedsConformanceExecution`) |
| 44 | `free-choice-petri-nets-1995.pdf` | Free-Choice Petri Nets | 1995 | Desel, Esparza | `WF_NET_SOUNDNESS` | WfNetConst<SOUNDNESS>, free-choice subclass marker, free-choice soundness decidability in polynomial time, place-sharing condition, conflict-free cluster, T-component, S-component | Free-choice nets are a subclass of Petri nets where soundness is decidable in polynomial time (vs. PSPACE for general WF-nets); the free-choice property is a named structural law that should be typed as a const-generic subclass marker. `WfNetConst<SOUNDNESS>` is the existing soundness surface; a `FreeChoiceMarker` const-generic tag would distinguish free-choice nets (polynomial soundness) from general WF-nets (PSPACE soundness). Soundness verification algorithm execution graduates to wasm4pm | `src/petri.rs` — `WfNetConst<SOUNDNESS>` with non-forgeable witness path; `FreeChoiceMarker` const-generic tag not yet typed as a distinct subclass | Zero-cost possible: `FreeChoiceMarker` as a const-generic bool tag on `WfNetConst`; `WfNetConst<SOUNDNESS, IS_FREE_CHOICE>` would be a distinct type from plain `WfNetConst<SOUNDNESS>` | `compile_pass/wfnet_with_soundness_witness.rs` | `compile_fail/wfnet_forged_soundness.rs` | `wfnet_forged_soundness.stderr` | `FreeChoiceMarker` const-generic tag to distinguish free-choice soundness path from general WF-net soundness path | `COVERED_BY_GRADUATION_BOUNDARY` — `WfNetConst<SOUNDNESS>` soundness witness path typed in compat; free-choice net subclass (`FreeChoiceMarker` const-generic tag) would make the polynomial soundness path distinct; soundness verification execution graduates to wasm4pm (`NeedsConformanceExecution`) |
| 43 | `powl-2023.pdf` | POWL: Partially Ordered Workflow Language | 2023 | Kourani, van der Aalst | `POWL` | PowlNodeKind (StrictPartialOrder, OperatorNode, Transition, SilentTransition), ChoiceGraphEdge, OrderEdge, partial order over workflow nodes, loop/choice/parallel operators, silent transitions as first-class POWL nodes | POWL defines four node kinds as structural laws: `StrictPartialOrder`, `OperatorNode`, `Transition`, and `SilentTransition`; each is a distinct type, not a free string. The choice graph edge (`ChoiceGraphEdge`) and order edge (`OrderEdge`) are distinct types — confusing them is a compile error. `SilentTransition` is a first-class POWL node, not an annotation. The `PowlPaper` witness in `src/witness.rs` is the non-forgeable receipt for POWL node-kind laws | `src/powl.rs` — `PowlNodeKind` enum (`StrictPartialOrder`, `OperatorNode`, `Transition`, `SilentTransition`), `ChoiceGraphEdge`, `OrderEdge`, `TreeProjectable` sealed trait; `src/witness.rs` — `PowlPaper` witness marker; `src/nightly_foundry.rs` — `powl_law` surface | Zero-cost: `PowlNodeKind` variants as distinct structural types; `ChoiceGraphEdge` and `OrderEdge` as distinct newtypes; `TreeProjectable` sealed trait prevents non-projectable POWL from claiming tree projection | `compile_pass/powl_choice_graph.rs`, `compile_pass/powl_process_tree_projectable.rs` | `compile_fail/powl_order_edge_choice_confusion.rs`, `compile_fail/powl_silent_tree_projection.rs` | `powl_order_edge_choice_confusion.stderr`, `powl_silent_tree_projection.stderr` | None — all key POWL formal objects reified | `COVERED_BY_TYPE` — `PowlNodeKind` (StrictPartialOrder/OperatorNode/Transition/SilentTransition), `ChoiceGraphEdge`, `OrderEdge`, `TreeProjectable` sealed trait, `PowlPaper` witness, and `powl_law` surface in `src/nightly_foundry.rs` implement POWL type laws |
| 42 | `oc-dfg-2020.pdf` | A Directly Follows Graph for Object-Centric Process Mining | 2020 | Berti, van der Aalst | `DFG_OBJECT_CENTRIC` | Dfg, DfgNode, DfgEdge, DfgWeight, OcDfgEdge, object-type annotation per edge, directly-follows frequency/performance per object type | OC-DFG extends the flat DFG by annotating each edge with a PhantomData object-type marker; this distinguishes `DfgEdge` (classic flat DFG) from `OcDfgEdge<ObjectType>` (object-centric DFG edge). The edge annotation is a structural law, not a runtime property. OC-DFG computation from OCEL logs graduates to wasm4pm | `src/dfg.rs` — `Dfg`, `DfgNode`, `DfgEdge`, `DfgWeight`; `OcDfgEdge` with `PhantomData` object-type marker needed to complete object-centric edge distinction | Zero-cost possible: `OcDfgEdge<ObjectType>` as newtype over `DfgEdge` with `PhantomData<ObjectType>`; flat vs. OC-DFG edges are distinct types | `compile_pass/dfg_shape.rs` | `compile_fail/dfg_wrong_edge_type.rs` | `dfg_wrong_edge_type.stderr` | `OcDfgEdge<ObjectType>` with PhantomData object-type marker to complete OC-DFG edge annotation | `COVERED_BY_GRADUATION_BOUNDARY` — `Dfg`/`DfgNode`/`DfgEdge`/`DfgWeight` typed in compat; `OcDfgEdge` with PhantomData object-type marker needed; OC-DFG computation from OCEL graduates to wasm4pm (`NeedsObjectCentricQueryExecution`) |
| 41 | `inductive-miner-infrequent-2014.pdf` | Inductive Miner — Infrequent (IMf) | 2014 | Leemans, Fahland, van der Aalst | `PROCESS_TREES_INDUCTIVE` | ProcessTree, TypedLoopNode<ARITY>, ProcessOperator, noise-filtering threshold parameter, infrequency-filtered directly-follows relation | IMf adds a noise-filtering threshold to the base IM algorithm (see #27); the output shape is identical to base IM — a block-structured process tree with the same ProcessTree/TypedLoopNode/ProcessOperator types. The threshold parameter is a runtime concern; the structural output types are shared with base IM. Inductive mining execution (with or without noise filter) graduates to wasm4pm | `src/process_tree.rs` — `ProcessTree`, `TypedLoopNode<ARITY>` with `Require<{ ARITY == 2 }>: IsTrue`, `ProcessOperator`; `src/powl.rs` — `TreeProjectable` sealed trait (same as #27) | Zero-cost: `TypedLoopNode<ARITY>` const-generic arity check; `ProcessOperator` as `ConstParamTy` — identical surface to base IM (#27) | `compile_pass/process_tree_loop_arity_2.rs` | `compile_fail/process_tree_bad_loop_arity.rs` | `process_tree_bad_loop_arity.stderr` | None for structure; inductive mining with noise-filtering execution graduates to wasm4pm | `COVERED_BY_GRADUATION_BOUNDARY` — process tree shapes (ProcessTree/TypedLoopNode/ProcessOperator) are identical to base IM (#27) and already typed in compat; IMf noise-filtering threshold and inductive mining execution graduates to wasm4pm (`NeedsDiscovery`) |
| 40 | `precision-etc-2010.pdf` | Measuring Precision of Modeled Behavior | 2010 | Munoz-Gama, Carmona | `CONFORMANCE_ALIGNMENT` | PrecisionConst, ETC precision (Escaping Edges), anti-alignment precision, precision formula (escaping edges ratio), Between01<NUM,DEN> bounds, state-space enumeration | ETC precision is a named formula bounded in [0,1]; an anti-alignment precision value that escapes the unit interval is a type error. The `PrecisionConst` and `Between01` metric shapes are the structural receipt for both ETC precision and anti-alignment precision; escaping-edges enumeration and anti-alignment computation graduate to wasm4pm | `src/conformance.rs` — `Metric<KIND, NUM, DEN>` with `Between01<NUM, DEN>` bounds; `PrecisionConst` const-generic metric type; `metric_out_of_bounds` compile-fail law rejects NUM > DEN | Zero-cost: `PrecisionConst` with `Between01<NUM, DEN>` bound enforces the Munoz-Gama & Carmona (2010) precision formula at the type level | `compile_pass/ocpq_cardinality_valid_bounds.rs` | `compile_fail/metric_out_of_bounds.rs` | `metric_out_of_bounds.stderr` | None for metric shapes; ETC precision computation and anti-alignment precision graduates to wasm4pm | `COVERED_BY_GRADUATION_BOUNDARY` — `PrecisionConst` with `Between01` bounds typed in compat; ETC (Escaping Edges) precision and anti-alignment precision computation graduates to wasm4pm (`NeedsConformanceExecution`) |
| 39 | `token-replay-fitness-2019.pdf` | Aligning Observed and Modeled Behavior (token-based replay) | 2019 | Berti, van der Aalst | `CONFORMANCE_ALIGNMENT` | FitnessConst, token-replay fitness formula (missing tokens / remaining tokens), produced/consumed token counts, replay trace, enabled transition | Token-replay fitness is a named formula bounded in [0,1]: `fitness = 0.5 * (1 - missing/produced) + 0.5 * (1 - remaining/consumed)`; a fitness value that escapes the unit interval is a type error. The `FitnessConst` and `Between01` metric shapes are the structural receipt for this formula; token firing (produce/consume/missing/remaining counts) graduates to wasm4pm | `src/conformance.rs` — `Metric<KIND, NUM, DEN>` with `Between01<NUM, DEN>` bounds; `FitnessConst` const-generic metric type; `metric_out_of_bounds` compile-fail law rejects NUM > DEN | Zero-cost: `FitnessConst` with `Between01<NUM, DEN>` bound enforces the Berti & van der Aalst (2019) token-replay fitness formula at the type level | `compile_pass/ocpq_cardinality_valid_bounds.rs` | `compile_fail/metric_out_of_bounds.rs` | `metric_out_of_bounds.stderr` | None for metric shapes; token replay execution (fire transitions, count missing/remaining tokens) graduates to wasm4pm | `COVERED_BY_GRADUATION_BOUNDARY` — `FitnessConst` with `Between01` bounds typed in compat; token replay execution (fire transitions, count missing/remaining tokens) graduates to wasm4pm (`NeedsConformanceExecution`) |
| 45 | `heuristics-miner-2011.pdf` | Heuristics Miner: Mining Process Models from Imperfect Event Logs | 2011 | Weijters, Ribeiro | `DFG_OBJECT_CENTRIC` | Dfg, DfgEdge, DfgWeight, causal net (heuristics net), dependency measure, threshold-based arc selection, short-loop handling | Heuristics Miner outputs a causal net (heuristics net), not a Petri net — this is a structurally distinct output shape: places are implicit, arcs carry dependency measures, and loops are represented differently from WF-net loops. `Dfg`, `DfgEdge`, and `DfgWeight` provide the DFG structural substrate; `CausalNet` shape (dedicated type for heuristics net) needs a home in `src/petri.rs` or a new `src/causal_net.rs` module. Heuristics Miner algorithm execution (dependency measure computation, threshold selection, arc filtering) graduates to wasm4pm | `src/dfg.rs` — `Dfg`, `DfgEdge`, `DfgWeight` provide the DFG substrate; `CausalNet` shape not yet typed | Zero-cost possible: `CausalNet` as a distinct struct from `WfNetConst`; dependency measure as `Between01<NUM, DEN>` const-generic fraction | None | None | None | `CausalNet` dedicated shape in `src/petri.rs` or new `src/causal_net.rs`; dependency measure as `Between01` const-generic | `COVERED_BY_GRADUATION_BOUNDARY` — `Dfg`/`DfgEdge`/`DfgWeight` in `src/dfg.rs` provide the structural DFG substrate; `CausalNet` (heuristics net) shape needs a dedicated type distinct from `WfNetConst`; Heuristics Miner discovery execution (dependency measure computation, threshold-based arc selection) graduates to wasm4pm (`NeedsDiscovery`) |
| 46 | `weske-bpm-2012.pdf` | Business Process Management: Concepts, Languages, Architectures | 2012 | Weske | `WORKFLOW_PATTERNS_BPMN` | BpmnElement, GatewayKind (XOR/AND/OR), BpmnSubprocess, gateway semantic law, execution semantics (token-passing, AND-join synchronisation, OR-join activation), process choreography | Weske (2012) is the canonical BPM textbook defining BPMN 2.0 formal semantics: XOR-split routes one token, AND-split duplicates tokens, OR-split activates a subset — each is a distinct semantic law beyond notation. `GatewayKind` in `src/bpmn.rs` covers the structural vocabulary; a `GatewaySemantics<KIND>` const-generic parameter would make XOR vs AND vs OR semantics a compile-time law, not a runtime choice. Execution semantics (token-passing, activation conditions) graduates to wasm4pm | `src/bpmn.rs` — `BpmnElement`, `GatewayKind` (XOR/AND/OR), `BpmnSubprocess`, `EventKind`; `GatewaySemantics<KIND>` const-generic not yet typed | Zero-cost possible: `GatewaySemantics<GatewayKind::Xor>` vs `GatewaySemantics<GatewayKind::And>` as distinct types on the existing `GatewayKind` const param | None | None | None | `GatewaySemantics<KIND>` const-generic type binding gateway structural kind to its semantic law | `COVERED_BY_GRADUATION_BOUNDARY` — `BpmnElement`/`GatewayKind`/`BpmnSubprocess` in `src/bpmn.rs` cover the structural BPMN 2.0 vocabulary; `GatewaySemantics<KIND>` const-generic would elevate gateway semantic law (XOR vs AND vs OR) to a compile-time distinction; BPMN execution semantics (token-passing, join activation) graduates to wasm4pm (`NeedsDiscovery`, `RebuildingProcessMiningLocally`) |
| 47 | `bpmn2-omg-spec-2011.pdf` | BPMN 2.0 — Business Process Model and Notation (OMG Specification) | 2011 | OMG | `WORKFLOW_PATTERNS_BPMN` | BpmnElement, GatewayKind (XOR/AND/OR), BpmnSubprocess, EventKind (Start/Intermediate/End), boundary event, message flow, pool, lane, data object | BPMN 2.0 OMG spec is the normative standard behind Real-Life BPMN (#11 in ledger); gateway types (XOR/AND/OR) and event kinds (Start/Intermediate/End) are structural laws distinct from execution semantics. The OMG spec formalises the BPMN metamodel — each element type is a distinct structural class, not a free string. This crate implements the structural metamodel surface; BPMN execution semantics (token-passing, formal operational semantics) graduates to wasm4pm | `src/bpmn.rs` — `BpmnElement`, `GatewayKind` (XOR/AND/OR), `BpmnSubprocess`, `EventKind`; gateway types and event kinds directly implement the OMG BPMN 2.0 metamodel | Zero-cost: `GatewayKind` and `EventKind` as `ConstParamTy` variants; each element kind is a distinct type, not a free string | None | None | None | None — structural metamodel surface already covered; BPMN execution semantics graduates | `COVERED_BY_TYPE` — `BpmnElement`, `GatewayKind` (XOR/AND/OR), `BpmnSubprocess`, `EventKind` in `src/bpmn.rs` directly implement the BPMN 2.0 OMG metamodel structural classes; gateway types are structural laws enforced at the type level; OMG spec is the normative source for #11 (Real-Life BPMN) already in ledger |
| 48 | `multi-perspective-pm-2011.pdf` | Multi-Perspective Process Mining | 2011 | van der Aalst | `XES_EVENT_LOG` | XesEvent resource perspective, data perspective attributes, time perspective (timestamp), case perspective (case-id), activity perspective (concept:name), ResourceAttribute, DataAttribute, perspective-specific extension declarations | Van der Aalst (2011) multi-perspective extends case-centric XES logs with resource, data, and time views: each perspective is a named XES extension. `XesEvent` in `src/xes.rs` carries timestamp and attribute map; `ResourceAttribute` and `DataAttribute` typed surfaces are partially present via XES extension attribute typing. Time dimension via timestamps is covered; resource and data perspectives as explicitly typed attribute namespaces are partial | `src/xes.rs` — `XesEvent` (timestamp, attribute map), `XesExtension`; resource perspective attributes and data perspective attributes not explicitly typed as distinct namespaces | Zero-cost possible: `ResourcePerspective` and `DataPerspective` as `PhantomData` extension markers on `XesEvent`; perspective-specific `XesExtension` variants | None | None | None | `ResourcePerspective` and `DataPerspective` as typed extension namespaces on `XesEvent`; perspective-scoped attribute typed surface. Law packet in NIGHTLY_TYPE_LAW.md. | `PARTIAL_WITH_REASON` — `XesEvent` carries timestamps (time perspective) and an attribute map (data perspective) in `src/xes.rs`; resource perspective and data perspective not yet typed as distinct named extension namespaces with `PhantomData` markers; van der Aalst (2011) multi-perspective framework requires each perspective to be a first-class extension, not a stringly-typed attribute key |
| 49 | `oc-pm-divergence-convergence-2020.pdf` | Object-Centric Process Mining: Dealing with Divergence and Convergence in Event Data | 2020 | van der Aalst, Berti | `OCEL_OBJECT_CENTRIC` | OcelLog, EventObjectLink, divergence (one case-id maps to many objects — over-counting), convergence (many case-ids share one object — under-counting), DivergenceWitness, ConvergenceWitness | Divergence (one-to-many case-to-object: events duplicated for each object) and convergence (many-to-one case-to-object: events merged across case boundaries) are named structural laws — each is a typed defect in flattened event logs that OCEL resolves structurally. `OcelLog` and `EventObjectLink` in `src/ocel.rs` provide the structural resolution; `DivergenceWitness` and `ConvergenceWitness` as named witness types in `src/witness.rs` would make these laws non-forgeable receipts. Law packet in NIGHTLY_TYPE_LAW.md. | `src/ocel.rs` — `OcelLog`, `OcelEvent`, `OcelObject`, `EventObjectLink` resolve divergence/convergence by construction; `src/witness.rs` — `DivergenceWitness` and `ConvergenceWitness` not yet typed | Zero-cost: `DivergenceWitness` and `ConvergenceWitness` as unit-struct witness types in `src/witness.rs`; each names the structural law it certifies | `compile_pass/ocel_event_object_relation.rs` | `compile_fail/ocel_e2o_missing_link.rs` | `ocel_e2o_missing_link.stderr` | `DivergenceWitness` and `ConvergenceWitness` named witness types in `src/witness.rs` | `COVERED_BY_TYPE` — `OcelLog`/`EventObjectLink` in `src/ocel.rs` structurally resolve divergence and convergence by making event-to-object links first-class; `DivergenceWitness` and `ConvergenceWitness` witness types would complete the named law receipt; underlying structural support (E2O link) is already present and compile-fail sealed |
| 50 | `dfg-mining-filtering-2019.pdf` | Directly-Follows Graphs: Mining and Filtering | 2019 | van der Aalst | `DFG_OBJECT_CENTRIC` | Dfg, DfgNode, DfgEdge, DfgWeight, directly-follows relation (activity A directly followed by activity B in a trace), frequency-weighted edges, threshold-based edge removal (filtering), start/end activity annotations | Foundational DFG paper: directly-follows is a structural relation, not a discovery algorithm output. `Dfg`, `DfgEdge`, and `DfgWeight` in `src/dfg.rs` implement this relation; dfg_engine_boundary_rejected compile-fail seals the engine boundary. DFG discovery and threshold-based filtering execution graduate to wasm4pm. | `src/dfg.rs` — `Dfg`, `DfgNode`, `DfgEdge`, `DfgWeight`; directly-follows structural relation fully typed | Zero-cost: `Dfg`/`DfgEdge`/`DfgWeight` as structural-only types; no runtime in compat | `compile_pass/dfg_shape.rs` | `compile_fail/dfg_wrong_edge_type.rs`, `compile_fail/dfg_engine_boundary_rejected.rs` | `dfg_wrong_edge_type.stderr`, `dfg_engine_boundary_rejected.stderr` | None — DFG structural relation fully covered; filtering execution graduates | `COVERED_BY_GRADUATION_BOUNDARY` — `Dfg`/`DfgNode`/`DfgEdge`/`DfgWeight` in `src/dfg.rs` implement the directly-follows structural relation (van der Aalst 2019); DFG discovery from event log and threshold-based filtering execution graduates to wasm4pm (`NeedsDiscovery`) |
| 51 | `process-querying-methods-2017.pdf` | Process Querying Methods | 2017 | Polyvyanyy, Ouyang, Barros, van der Aalst | `OCPQ_QUERYING` | ProcessQuery, QueryResult, process model querying framework, query language over process model shapes, behavioral/structural query predicates | Process querying defines a typed framework for issuing queries against process model shapes; a query is typed over the process model class it targets (Petri net, process tree, POWL); query results carry typed evidence of what was matched. OCPQ (see #6) extends this framework to the object-centric domain. Query execution graduates to wasm4pm | `src/ocpq.rs` — `OcpqQuery`, `OcpqResult`, `EventPredicate`, `ObjectPredicate`, `TemporalPredicate` provide the predicate witness marker surfaces that implement the typed query framework; Polyvyanyy et al. 2017 process querying framework — OCPQ extends to object-centric domain | Zero-cost possible: predicate witnesses as `PhantomData` markers; `OcpqQuery<ObjectTypes, EventTypes>` with const-generic type sets; typed result carrying predicate evidence | None | None | None | Explicit `ProcessQueryWitness` marker linking `OcpqQuery` surface to Polyvyanyy et al. 2017 process querying framework; `TemporalPredicate` coverage of temporal ordering axioms from the framework | `PARTIAL_WITH_REASON` — `src/ocpq.rs` defines `OcpqQuery`, `OcpqResult`, `EventPredicate`, `ObjectPredicate`, `TemporalPredicate` as predicate witness markers implementing the query framework surface; explicit `ProcessQueryWitness` binding to Polyvyanyy et al. 2017 framework not yet added; query execution over process model shapes graduates to wasm4pm |
| 52 | `event-abstraction-pm-2022.pdf` | Event Abstraction in Process Mining: Literature Review and Taxonomy | 2022 | Baier, De Weerdt, Senderovich | `XES_EVENT_LOG` | EventLog, Activity, Timestamp, AbstractionLevel marker type, event-to-activity mapping, abstraction taxonomy (clustering, aggregation, pattern-based), low-level vs high-level activity distinction | Event abstraction maps low-level events to high-level activities; the abstraction output is a structurally transformed event log where each event carries an activity typed at the abstracted level. `AbstractionLevel` as a marker type on `EventLog` or `Activity` would make the abstraction level a named structural property rather than a free string rename. Abstraction algorithm execution (clustering, aggregation, pattern matching) graduates to wasm4pm | `src/eventlog.rs` — `EventLog`, `Activity`, `Timestamp` provide the structural substrate for both low-level and high-level event logs; `AbstractionLevel` marker type not yet in `src/eventlog.rs` | Zero-cost possible: `AbstractionLevel` as a const-generic or `PhantomData` marker on `EventLog<LEVEL>`; `LowLevel` and `HighLevel` as unit-struct marker types | None | None | None | `AbstractionLevel` marker type in `src/eventlog.rs`; abstraction algorithm (clustering, aggregation, pattern-based lifting) graduates to wasm4pm | `COVERED_BY_GRADUATION_BOUNDARY` — `EventLog`, `Activity`, `Timestamp` in `src/eventlog.rs` provide the structural substrate; `AbstractionLevel` marker type not yet typed; event abstraction algorithm execution (clustering, aggregation, pattern-based lifting) graduates to wasm4pm (`NeedsDiscovery`) |
| 53 | `concept-drift-pm-2011.pdf` | Concept Drift Detection in Process Mining | 2011 | Bose, van der Aalst, Zliobaite, Pechenizkiy | `PREDICTION_DRIFT` | PredictionTarget, DriftWitness marker, temporal window over event log, change-point detection, process variant divergence, drift type taxonomy (sudden, gradual, recurring, incremental) | Concept drift detection requires temporal windowing over an event log and comparison of discovered process models across windows; the drift type taxonomy (sudden, gradual, recurring, incremental) defines named structural drift kinds. `DriftWitness` as a `PhantomData` marker on `PredictionTarget` would make the drift type a named structural property. Drift detection algorithm execution graduates to wasm4pm | `src/prediction.rs` — `PredictionTarget` structure provides the prediction surface substrate; `DriftWitness` marker type not yet typed in `src/prediction.rs` or `src/witness.rs` | Zero-cost possible: `DriftWitness` as a `PhantomData` marker on a `DriftAwarePrediction<DriftKind>`; `DriftKind` as a `ConstParamTy` enum with Sudden/Gradual/Recurring/Incremental variants | None | None | None | `DriftWitness` marker type in `src/prediction.rs` or `src/witness.rs`; `DriftKind` as `ConstParamTy` enum; temporal windowing execution graduates to wasm4pm | `COVERED_BY_GRADUATION_BOUNDARY` — `PredictionTarget` in `src/prediction.rs` provides the structural substrate; `DriftWitness` marker type not yet typed; drift detection algorithm execution (temporal windowing, change-point detection, model comparison) graduates to wasm4pm (`NeedsDiscovery`, `RebuildingProcessMiningLocally`) |
| 54 | `predictive-process-monitoring-survey-2019.pdf` | Predictive Process Monitoring: Methods and Applications | 2019 | Teinemaa, Dumas, Rosa, Maggi | `PREDICTION_DRIFT` | PredictionTarget, PredictionHorizon type param, PrefixLength type param, prefix-based feature extraction, outcome prediction, remaining time prediction, next activity prediction, ML model boundary | Predictive process monitoring requires prefix-based feature extraction and ML model inference; `PredictionTarget` defines what is being predicted (outcome, remaining time, next activity); `PredictionHorizon` and `PrefixLength` as const-generic type params would make the temporal scope of a prediction a named structural property. ML model training and inference graduate to wasm4pm | `src/prediction.rs` — `PredictionTarget` structure provides the prediction target surface; `PredictionHorizon` and `PrefixLength` type params not yet in `src/prediction.rs` | Zero-cost possible: `PredictionTarget<HORIZON, PREFIX_LEN>` with const-generic params; distinct types for outcome, remaining-time, and next-activity prediction targets | None | None | None | `PredictionHorizon` and `PrefixLength` const-generic type params in `src/prediction.rs`; ML model training and inference graduates to wasm4pm | `COVERED_BY_GRADUATION_BOUNDARY` — `PredictionTarget` in `src/prediction.rs` provides the prediction target substrate; `PredictionHorizon` and `PrefixLength` const-generic params not yet typed; prefix-based feature extraction and ML model inference graduate to wasm4pm (`RebuildingProcessMiningLocally`) |
| 55 | `magtempminer-temporal-constraints-2023.pdf` | MagTempMiner: Mining Temporal Constraints from Event Logs | 2023 | Cecconi, Maggi, De Leoni, Mendling | `DECLARE_CONSTRAINTS` | DeclareTemplate (response, precedence, chain-response with temporal bounds), TimedResponse, TimedPrecedence, time-bounded Declare variants, temporal constraint mining from event logs, time-distance distribution | MagTempMiner extends Declare constraint templates with time bounds (e.g., TimedResponse: activity A must be followed by B within N time units); time-bounded Declare variants (`TimedResponse`, `TimedPrecedence`) are structurally distinct from their untimed counterparts and should be named `DeclareTemplate` enum variants. Temporal constraint mining from event logs graduates to wasm4pm | `src/declare.rs` — `DeclareTemplate` enum covers response, precedence, chain-response variants; time-bounded variants (e.g., `TimedResponse`) not yet in `DeclareTemplate` enum | Zero-cost possible: `TimedResponse`, `TimedPrecedence`, `TimedChainResponse` as additional `DeclareTemplate` `ConstParamTy` variants; temporal bound as a const-generic duration parameter | None | None | None | `TimedResponse`, `TimedPrecedence`, `TimedChainResponse` variants added to `DeclareTemplate` enum in `src/declare.rs`; temporal constraint mining execution graduates to wasm4pm | `COVERED_BY_GRADUATION_BOUNDARY` — `DeclareTemplate` (response, precedence, chain-response variants) in `src/declare.rs` provides the untimed structural substrate; time-bounded Declare variants (`TimedResponse`, `TimedPrecedence`) not yet in `DeclareTemplate` enum; temporal constraint mining from logs graduates to wasm4pm (`NeedsDiscovery`) |
| 56 | `causal-nets-2011.pdf` | Causal Nets: A Modeling Language Tailored Towards the Upcoming Process Mining Spectrum | 2011 | van der Aalst | `DFG_OBJECT_CENTRIC` | CausalNet, CausalBinding, InputBinding, OutputBinding, dependency measure (arc weight), heuristics net output shape, binding set, implicit place semantics, dependency graph | C-nets (causal nets) are the direct output of the Heuristics Miner; they differ from WF-nets in that places are implicit and arcs carry dependency measures with explicit binding sets. `CausalNet`, `CausalBinding`, `InputBinding`, `OutputBinding` types are not yet defined in the crate; they belong in a dedicated `src/causal_net.rs` module. The C-net is a structurally distinct output shape from `WfNetConst`. Binding semantics execution (token-firing semantics over binding sets) graduates to wasm4pm | `src/dfg.rs` — `Dfg`, `DfgEdge`, `DfgWeight` provide the dependency graph substrate that C-nets extend; no dedicated `CausalNet` type yet in the crate | Zero-cost possible: `CausalNet` as a distinct struct from `WfNetConst`; `DependencyMeasure` as `Between01<NUM, DEN>` const-generic fraction; `InputBinding`/`OutputBinding` as typed set newtypes | None | None | None | `CausalNet`, `CausalBinding`, `InputBinding`, `OutputBinding` dedicated types in `src/causal_net.rs` or `src/petri.rs`; `DependencyMeasure` as `Between01` const-generic; binding semantics execution graduates to wasm4pm | `COVERED_BY_GRADUATION_BOUNDARY` — `Dfg`/`DfgEdge`/`DfgWeight` in `src/dfg.rs` provide the dependency graph substrate; dedicated `CausalNet`, `CausalBinding`, `InputBinding`, `OutputBinding` types not yet defined; `CausalNet` shape is structurally distinct from `WfNetConst` and must be a separate type; Heuristics Miner binding semantics execution graduates to wasm4pm (`NeedsDiscovery`) |
| 57 | `stochastic-conformance-leemans-2019.pdf` | Stochastic Conformance Checking with Stochastic Petri Nets | 2019 | Leemans, Syring, van der Aalst | `PETRI_NETS` | WfNetConst, StochasticArcWeight, ImmediateTransition, TimedTransition, stochastic conformance metric (earth mover distance), stochastic language, firing rate annotation | A stochastic Petri net extends a standard WF-net with per-transition firing rates; `ImmediateTransition` and `TimedTransition` are structurally distinct node kinds (not a boolean flag), and `StochasticArcWeight` is a structural annotation distinct from the plain bipartite arc. The earth mover distance conformance metric graduates to wasm4pm | `src/petri.rs` — `WfNetConst<SOUNDNESS>` provides the underlying net shape; `StochasticArcWeight`, `ImmediateTransition`, `TimedTransition` not yet typed in `src/petri.rs` | Zero-cost possible: `StochasticArcWeight<NUM, DEN>` as a `Between01`-bounded fraction; `ImmediateTransition` and `TimedTransition` as distinct unit-struct markers on `WfNetConst` | None | None | None | `StochasticArcWeight<NUM, DEN>` newtype bounded by `Between01`; `ImmediateTransition` and `TimedTransition` as distinct structural markers in `src/petri.rs`; earth mover distance computation graduates to wasm4pm | `PARTIAL_WITH_REASON` — `WfNetConst<SOUNDNESS>` in `src/petri.rs` provides the base net shape; `StochasticArcWeight`, `ImmediateTransition`, and `TimedTransition` structural annotations not yet typed; stochastic conformance metric (earth mover distance) graduates to wasm4pm (`NeedsConformanceExecution`) |
| 58 | `ocel-extraction-materialization-ghahfarokhi-2021.pdf` | Object-Centric Process Mining: Extracting and Materializing Object-Centric Event Logs | 2021 | Ghahfarokhi, Park, Berti, van der Aalst | `OCEL_OBJECT_CENTRIC` | OcelLog, OcelEvent, OcelObject, EventObjectLink, ExtractionProjection, relational flattening, materialization algorithm, join logic, LossPolicy | Extraction from relational databases (flattening multiple tables to an OCEL) involves join logic that introduces object multiplicity — an `ExtractionProjection` with a named `LossPolicy` is required to account for the loss of non-joined rows. The materialization algorithm (building OCEL from SQL joins) graduates to wasm4pm | `src/ocel.rs` — `OcelLog`, `OcelEvent`, `OcelObject`, `EventObjectLink`; `src/loss.rs` — `LossPolicy`, `LossReport`; `ExtractionProjection` type not yet defined | Zero-cost possible: `ExtractionProjection<POLICY>` as a const-generic typed projection recording the relational source and join strategy | None | None | None | `ExtractionProjection<POLICY>` type in `src/ocel.rs` or `src/loss.rs` named after the relational extraction join; materialization algorithm graduates to wasm4pm | `COVERED_BY_GRADUATION_BOUNDARY` — `OcelLog`/`OcelEvent`/`OcelObject`/`EventObjectLink` in `src/ocel.rs` and `LossPolicy`/`LossReport` in `src/loss.rs` provide the structural surface; `ExtractionProjection` with named `LossPolicy` not yet typed; relational join logic and materialization algorithm execution graduate to wasm4pm (`NeedsDiscovery`) |
| 59 | `unfolding-conformance-armas-cervantes-2017.pdf` | Conformance Checking Using Petri Net Unfoldings | 2017 | Armas-Cervantes, Baldan, Dumas, Garcia-Banuelos | `CONFORMANCE_ALIGNMENT` | WfNetConst, BranchingProcess, UnfoldingPrefix, condition, event (unfolding sense), cut-off criterion, maximal conflict-free set | A Petri net unfolding (branching process) is a structurally distinct representation from the original net: conditions correspond to place instances, events to transition firings, and the prefix is a finite acyclic structure. `BranchingProcess` and `UnfoldingPrefix` are structural types not yet modeled in `src/petri.rs`; unfolding computation and conformance replay over the unfolding graduate to wasm4pm | `src/petri.rs` — `WfNetConst<SOUNDNESS>` is the input net shape; `BranchingProcess`, `UnfoldingPrefix`, condition and event types not yet in `src/petri.rs` | Zero-cost possible: `BranchingProcess<N>` as a const-generic acyclic structure; `UnfoldingPrefix` as a distinct type from `WfNetConst`; condition and event as distinct newtypes from place and transition | None | None | None | `BranchingProcess` and `UnfoldingPrefix` structural types in `src/petri.rs`; unfolding computation and conformance checking over branching process graduate to wasm4pm | `COVERED_BY_GRADUATION_BOUNDARY` — `WfNetConst<SOUNDNESS>` in `src/petri.rs` provides the input net shape; `BranchingProcess`, `UnfoldingPrefix`, condition and event types not yet typed; unfolding computation and conformance replay over branching process graduate to wasm4pm (`NeedsConformanceExecution`) |
| 60 | `mining-roles-song-vanderdaalst-2008.pdf` | Mining Roles from Event Logs | 2008 | Song, van der Aalst | `XES_EVENT_LOG` | EventLog, Activity, ResourceAttribute, ResourceGrouping, role (cluster of resources sharing activity patterns), role mining algorithm, resource-activity matrix | Resource roles are a structural attribute of event data — each event may carry a resource identifier (org:resource in XES), and a role is a grouping of resources with similar activity execution patterns. `RoleAttribute` and `ResourceGrouping` types are not yet in `src/eventlog.rs`; the role mining algorithm (clustering over the resource-activity matrix) graduates to wasm4pm | `src/eventlog.rs` — `EventLog`, `Activity` provide the substrate; `src/xes.rs` — `XesEvent` carries org:resource attribute as untyped attribute map entry; `RoleAttribute` and `ResourceGrouping` not yet defined | Zero-cost possible: `RoleAttribute` as a typed newtype over resource identifier; `ResourceGrouping` as a typed set of `RoleAttribute` instances bound to a named role | None | None | None | `RoleAttribute` and `ResourceGrouping` typed newtypes in `src/eventlog.rs`; role mining algorithm (resource-activity matrix, clustering) graduates to wasm4pm | `COVERED_BY_GRADUATION_BOUNDARY` — `EventLog`/`Activity` in `src/eventlog.rs` and `XesEvent` org:resource attribute in `src/xes.rs` provide the structural substrate; `RoleAttribute` and `ResourceGrouping` typed newtypes not yet defined; role mining algorithm execution (resource-activity matrix, clustering) graduates to wasm4pm (`NeedsDiscovery`) |
| 61 | `process-performance-analysis-bpi-2005.pdf` | Process Performance Analysis Using Business Process Intelligence | 2005 | Grigori, Casati, Castellanos, Dayal, Sayal, Shan | `CONFORMANCE_ALIGNMENT` | ConformanceMetric, PerformanceMetric, waiting time, service time, throughput, cycle time, Between01-bounded relative ratios, absolute time metric, PerformanceMetricKind | Performance metrics (waiting time, service time, throughput, cycle time) are structurally distinct from conformance metrics: relative ratios can be `Between01`-bounded, but absolute time metrics need a `PerformanceMetric` type that carries a time unit rather than a unit-interval fraction. `PerformanceMetric<KIND, UNIT>` as a const-generic type distinct from `Metric<KIND, NUM, DEN>` is the missing surface; performance computation graduates to wasm4pm | `src/conformance.rs` — `Metric<KIND, NUM, DEN>` with `Between01<NUM, DEN>` bounds covers relative conformance ratios; `PerformanceMetric<KIND, UNIT>` type not yet defined; `src/dfg.rs` — `DfgWeight` carries frequency counts but not time-unit-bearing performance values | Zero-cost possible: `PerformanceMetric<KIND, UNIT>` as a distinct newtype from `Metric<KIND, NUM, DEN>`; `PerformanceMetricKind` as a `ConstParamTy` enum (WaitingTime, ServiceTime, Throughput, CycleTime); `TimeUnit` as a `ConstParamTy` enum (Seconds, Minutes, Hours) | None | None | None | `PerformanceMetric<KIND, UNIT>` newtype and `PerformanceMetricKind` `ConstParamTy` enum in `src/conformance.rs`; performance computation graduates to wasm4pm | `COVERED_BY_GRADUATION_BOUNDARY` — `Metric<KIND, NUM, DEN>` with `Between01` bounds in `src/conformance.rs` covers relative conformance ratios; `PerformanceMetric<KIND, UNIT>` type for absolute time metrics not yet defined; performance computation (waiting time, service time, throughput aggregation) graduates to wasm4pm (`NeedsConformanceExecution`, `NeedsObjectCentricQueryExecution`) |
| 62 | `anti-alignments-carmona-cortadella-2014.pdf` | Anti-Alignments — Measuring the Degree of Underfitting in Process Mining | 2014 | Carmona, Cortadella | `CONFORMANCE_ALIGNMENT` | PrecisionConst, Between01, anti-alignment (maximal-length trace not fitting the model), underfitting degree, anti-alignment distance metric | Anti-alignments are the formal dual of alignments: where an alignment finds the closest model trace to an observed log trace, an anti-alignment finds the maximal-length trace that is NOT in the model — measuring underfitting. The underfitting degree metric is bounded in [0,1] and belongs to the `PrecisionConst` metric family already typed via `Between01<NUM, DEN>` in `src/conformance.rs`. Anti-alignment computation (maximal-length non-fitting trace search) graduates to wasm4pm | `src/conformance.rs` — `PrecisionConst` and `Between01<NUM, DEN>` provide the metric shape for the underfitting degree; `Metric<KIND, NUM, DEN>` is the carrier; anti-alignment search execution not present (graduates to wasm4pm) | Zero-cost: `PrecisionConst` with `Between01<NUM, DEN>` bound already enforces the unit-interval metric law that underfitting degree obeys | `compile_pass/ocpq_cardinality_valid_bounds.rs` | `compile_fail/metric_out_of_bounds.rs` | `metric_out_of_bounds.stderr` | None for metric shapes; anti-alignment computation (maximal-length non-fitting trace search) must graduate | `COVERED_BY_GRADUATION_BOUNDARY` — `PrecisionConst` and `Between01<NUM, DEN>` metric shapes in `src/conformance.rs` type the anti-alignment underfitting degree metric; anti-alignment computation (maximal-length trace not in model) graduates to wasm4pm (`NeedsConformanceExecution`); Carmona & Cortadella 2014 |
| 63 | `xes-ieee-1849-2016.pdf` | XES Standard — IEEE 1849-2016 (first edition) | 2016 | IEEE Standards Association | `XES_EVENT_LOG` | XesLog, XesTrace, XesEvent, CaseCentricMarker, XesExtension — same formal shapes as the 2023 second edition | IEEE 1849-2016 is the first edition of the XES standard; it defines the same formal schema (XesLog, XesTrace, XesEvent, case-centric structure, extension declarations) that was revised and superseded by IEEE 1849-2023 (ledger entry #26). The `Xes1849` witness in `src/witness.rs` covers both editions — both define the same structural laws. The 2016 edition is the predecessor background reference for #26 | `src/xes.rs` — `XesLog`, `XesTrace`, `XesEvent`, `CaseCentricMarker`, `XesExtension`; `src/witness.rs` — `Xes1849` witness covers IEEE 1849-2016 and IEEE 1849-2023 identically | Same zero-cost Rust surfaces as #26 | Same fixtures as #26 | Same fixtures as #26 | Same `.stderr` files as #26 | None — same formal shapes as #26; 2016 edition is a structural subset of the 2023 revision | `DUPLICATE_OR_BACKGROUND` — duplicates ledger entry #26 (XES IEEE 1849-2023); the `Xes1849` witness in `src/witness.rs` covers both 2016 and 2023 editions; same formal shapes (XesLog/XesTrace/XesEvent/CaseCentricMarker/XesExtension); 2016 first edition is background reference only |
| 64 | `temporal-profile-conformance-stertz-2020.pdf` | Temporal Profile Conformance Checking | 2020 | Stertz, Rinderle-Ma, Rinderle | `CONFORMANCE_ALIGNMENT` | TemporalProfile, TimeDelta, ZScore, AVG/STD per activity-pair, zeta-value metric (standard deviations from mean), Between01-bounded deviation score | Temporal profile conformance defines, for each pair of activities (A, B), a mean and standard deviation of the observed time distance: `TP(A,B) = (AVG, STD)`. A zeta-value measures how many standard deviations the actual time distance deviates from the mean — a zeta-value metric is bounded by the user-supplied tolerance threshold. `Metric<KIND, NUM, DEN>` with `Between01` provides the metric shape substrate; `TemporalProfile`, `TimeDelta`, and `ZScore` named types are not yet in `src/conformance.rs`. Zeta-value conformance checking execution graduates to wasm4pm | `src/conformance.rs` — `Metric<KIND, NUM, DEN>` with `Between01<NUM, DEN>` bounds provides the metric substrate; `TemporalProfile` (map from activity-pair to AVG/STD), `TimeDelta` (typed time-distance newtype), and `ZScore` (typed deviation newtype) not yet defined | Zero-cost possible: `TemporalProfile<ActivityPair>` as a struct with const-generic activity-pair type param; `TimeDelta` as a typed duration newtype; `ZScore` as a newtype over a rational fraction with `Between01` bound for normalized deviation | `compile_pass/ocpq_cardinality_valid_bounds.rs` | `compile_fail/metric_out_of_bounds.rs` | `metric_out_of_bounds.stderr` | `TemporalProfile<ActivityPair>` struct, `TimeDelta` newtype, `ZScore` newtype in `src/conformance.rs`; zeta-value conformance checking execution graduates to wasm4pm; Stertz et al. 2020 | `PARTIAL_WITH_REASON` — `Metric<KIND, NUM, DEN>` with `Between01<NUM, DEN>` in `src/conformance.rs` provides the metric shape substrate that zeta-value scores obey; `TemporalProfile` (AVG/STD per activity-pair), `TimeDelta` (typed time-distance), and `ZScore` (typed deviation measure) structural types not yet defined; zeta-value conformance checking execution (deviation from temporal profile) graduates to wasm4pm (`NeedsConformanceExecution`); Stertz et al. 2020 |
| 65 | `approximate-semantic-process-querying.pdf` | Approximate Semantic Process Querying | n/a | n/a | `OCPQ_QUERYING` | OcpqQuery, OcpqResult, SimilarityScore<NUM,DEN> (Between01-bounded similarity metric), approximate (similarity-scored) query results, semantic query matching | Approximate semantic process querying extends exact process querying with a similarity score: instead of returning only exact matches, an approximate query returns results with a `SimilarityScore<NUM, DEN>` (a `Between01`-bounded fraction representing match quality). `OcpqQuery` and `OcpqResult` in `src/ocpq.rs` cover the query shape; `SimilarityScore<NUM, DEN>` as a `Between01`-bounded metric newtype is not yet in `src/ocpq.rs`. Approximate query execution (similarity computation) graduates to wasm4pm | `src/ocpq.rs` — `OcpqQuery`, `OcpqResult`, `EventPredicate`, `ObjectPredicate` provide the query framework; `SimilarityScore<NUM, DEN>` typed result metric not yet in `src/ocpq.rs`; `src/conformance.rs` — `Between01<NUM, DEN>` provides the metric bound law | Zero-cost possible: `SimilarityScore<NUM, DEN>` as a newtype over `Between01<NUM, DEN>` in `src/ocpq.rs`; semantically distinct from `PrecisionConst` or `FitnessConst` because it scores query-result match quality rather than conformance | None | None | None | `SimilarityScore<NUM, DEN>` `Between01`-bounded newtype in `src/ocpq.rs`; approximate query execution (similarity computation over process model shapes) graduates to wasm4pm | `COVERED_BY_GRADUATION_BOUNDARY` — `OcpqQuery` and `OcpqResult` in `src/ocpq.rs` cover the query structure; `SimilarityScore<NUM, DEN>` as a `Between01`-bounded metric for approximate query results is the named missing surface; approximate query execution (similarity-scored matching) graduates to wasm4pm (`NeedsObjectCentricQueryExecution`) |
| 66 | `replay-techniques-conformance-vanderdaalst-2010.pdf` | Replay Techniques for Conformance Analysis | 2010 | van der Aalst, Adriansyah, de Medeiros | `CONFORMANCE_ALIGNMENT` | FitnessConst, ReplayMode (TokenReplay / Alignment), token-replay fitness formula, alignment-based replay, synchronous product net, move-on-log, move-on-model | Replay techniques defines two canonical replay modes as a named structural law: `TokenReplay` (fire tokens through the net, count missing/remaining) and `Alignment` (synchronous product net, optimal edit distance). Each mode produces a `FitnessConst` metric bounded in [0,1]. The `FitnessConst` and `Between01` shapes are the structural surface; a `ReplayMode` enum with `TokenReplay | Alignment` variants is the named two-mode structural law. Replay execution (token firing, A* alignment search) graduates to wasm4pm | `src/conformance.rs` — `FitnessConst` and `Between01<NUM, DEN>` type the fitness metric shape; `ReplayMode` enum with `TokenReplay` / `Alignment` variants not yet in `src/conformance.rs` | Zero-cost possible: `ReplayMode` as a `ConstParamTy` enum with `TokenReplay` and `Alignment` variants; a `FitnessConst` parameterized by `ReplayMode` would distinguish token-replay fitness from alignment-based fitness at the type level | `compile_pass/ocpq_cardinality_valid_bounds.rs` | `compile_fail/metric_out_of_bounds.rs` | `metric_out_of_bounds.stderr` | `ReplayMode` `ConstParamTy` enum with `TokenReplay` / `Alignment` variants in `src/conformance.rs`; replay execution (token firing, A* alignment search) graduates to wasm4pm; van der Aalst et al. 2010 | `COVERED_BY_GRADUATION_BOUNDARY` — `FitnessConst` with `Between01<NUM, DEN>` in `src/conformance.rs` types the replay fitness metric; `ReplayMode` enum (`TokenReplay` / `Alignment`) is the named two-mode structural law not yet typed; token-replay execution (produce/consume/missing/remaining token counts) and alignment-based replay execution (synchronous product net, A* cost search) graduate to wasm4pm (`NeedsConformanceExecution`); van der Aalst et al. 2010 |

---

## Shard Assignments

### LANE_B: Event Format / OCEL / XES / DFG

Papers whose primary formal objects are event logs, event data formats, or object-centric event structures. These drive `src/eventlog.rs`, `src/ocel.rs`, `src/xes.rs`, `src/dfg.rs`, `src/ids.rs`.

| # | Title | Gap |
|---|---|---|
| 5 | Object-Centric Analysis of XES Event Logs (OCED/SPARQL) | XES→OCED interop loss surface |
| 6 | OCPQ: Object-Centric Process Querying & Constraints | `ObjectTypeSet`/`EventTypeSet` const params; typed constraint violation |
| 7/8 | PM4Py (both editions) | PM4Py format canon reference; no gap — covered by graduation boundary |
| 45 | Heuristics Miner (Weijters & Ribeiro, 2011) | `CausalNet` dedicated shape in `src/petri.rs` or new `src/causal_net.rs`; dependency measure as `Between01` const-generic |
| 48 | Multi-Perspective Process Mining (van der Aalst, 2011) | `ResourcePerspective` and `DataPerspective` as typed extension namespaces on `XesEvent` |
| 49 | OC-PM: Divergence and Convergence (van der Aalst & Berti, 2020) | `DivergenceWitness` and `ConvergenceWitness` named witness types in `src/witness.rs` |
| 50 | Directly-Follows Graphs: Mining and Filtering (van der Aalst, 2019) | No gap — DFG structural relation fully covered; filtering execution graduates |

### LANE_C: Process Model / WF-net / POWL / Conformance / Prediction

Papers whose primary formal objects are process models, structural laws, conformance metrics, or prediction frameworks. These drive `src/petri.rs`, `src/powl.rs`, `src/conformance.rs`, `src/prediction.rs`, `src/process_tree.rs`, `src/declare.rs`.

| # | Title | Gap |
|---|---|---|
| 1 | Compliance-Aware Predictive Process Monitoring | `ComplianceConstraintWitness<W>`; compliance metric as `Between01` |
| 3 | Hierarchical Decomposition of Separable Workflow-Nets | `SeparableWfNet` marker; `WfNet2Powl` conversion witness; POWL 2.0 choice graph type |
| 11 | Real-Life BPMN 4th edition | BPMN gateway type witnesses; pool/lane structural typing |
| 14 | Workflow Patterns: The Definitive Guide | Named `WorkflowPattern` type as `ConstParamTy`; pattern coverage claim on WF-net |
| 18 | YAWL: Yet Another Workflow Language | WF-net soundness fully covered; cancellation region and multi-instance types missing |
| 46 | Business Process Management (Weske, 2012) | `GatewaySemantics<KIND>` const-generic binding gateway structural kind to semantic law |
| 47 | BPMN 2.0 OMG Specification | No gap — structural metamodel covered by `BpmnElement`/`GatewayKind`/`EventKind` in `src/bpmn.rs` |

### BACKGROUND

Papers that provide background context, tool reference, or domain application knowledge but impose no new type-law burden on this crate.

| # | Title | Reason |
|---|---|---|
| 9 | PMAx Agentic Framework | Runtime system; schema surface covered by graduation boundary. Graduates: Engineer/Analyst agent execution (`NeedsDiscovery`, `NeedsConformanceExecution`, `RebuildingProcessMiningLocally`) |
| 10 | Process Mining for Healthcare | Domain application; all formal objects in standard PM canon |
| 16 | YAWL Technical Manual v5 | Engine API; graduation boundary. Graduates: worklet service dispatch, Interface B observer gateway, custom service API (`NeedsConformanceExecution`, `NeedsReceipts`, `RebuildingProcessMiningLocally`) |
| 17 | YAWL Open Source BPMS | Overlaps with #16 and #18; background |
| 19 | YAWL Technical Manual 5.0 | Duplicate of #16 |
| 21 | No AI Without PI! | OCPM vision paper. Graduates: OCED discovery, compliance execution, performance computation, AI pipeline (`NeedsDiscovery`, `NeedsConformanceExecution`, `NeedsObjectCentricQueryExecution`, `NeedsReceipts`) |

### OUT_OF_SCOPE

Papers with no process-mining type-law relevance for this crate.

| # | Title | Reason |
|---|---|---|
| 2 | Hands-On Python for DevOps | Python DevOps book |
| 4 | How Anthropic Teams Use Claude Code | Anthropic internal tool report |
| 12 | sAirflow: Adopting Serverless in a Legacy Workflow Scheduler | HPC/serverless infrastructure |
| 13 | Why Automate This? | HRI/sociology |
| 15 | Workflows Community Summit 2024 | Scientific/HPC workflows (different domain) |
| 20 | YAWL User Manual 5.1 | End-user manual |
| 22 | Ontology-Driven M2M Transformation of Workflow Specifications | M2M toolchain paper; BPMN target already in canon |
| 23 | Procedure Model for Building Knowledge Graphs | KG construction methodology; no PM objects |
| 24 | RDFGraphGen: RDF Graph Generator based on SHACL Shapes | RDF tooling; no process-mining relevance |

---

## Coverage Summary

| Status | Count | Papers |
|---|---|---|
| `COVERED_BY_TYPE` | 8 | #11 (BPMN), #14 (Workflow Patterns), #18 (YAWL/WF-net soundness), #33 (Murata Petri Nets), #34 (WF-net Soundness 1998), #35 (OCEL 1.0), #47 (BPMN 2.0 OMG Spec), #49 (OC-PM Divergence/Convergence) |
| `COVERED_BY_GRADUATION_BOUNDARY` | 21 | #7, #8 (PM4Py), #9 (PMAx), #16 (YAWL Technical Manual), #21 (No AI Without PI!), #27 (Inductive Miner), #29 (Alpha Miner), #30 (Log Skeleton), #36 (OC-Petri Net Discovery), #37 (PM Book 2011), #38 (Conformance Checking Book), #39 (Token Replay), #40 (ETC Precision), #41 (IMf), #42 (OC-DFG), #45 (Heuristics Miner), #46 (Weske BPM Textbook), #50 (DFG Mining & Filtering), #52 (Event Abstraction), #53 (Concept Drift Detection), #54 (Predictive PM survey), #55 (MagTempMiner), #56 (Causal Nets), #58 (OCEL Extraction/Materialization), #59 (Petri Net Unfoldings), #60 (Mining Roles), #61 (Process Performance Analysis) — each entry names specific `GraduationReason` variants |
| `PARTIAL_WITH_REASON` | 6 | #1 (PPM compliance: fixture exists, witness law complete), #3 (POWL 2.0: SeparableWfNet added, WfNet2Powl bridge missing), #5 (XES/OCED: projection surface complete), #48 (Multi-Perspective PM: time perspective covered; resource/data perspectives need typed namespaces), #51 (Process Querying Methods: predicate witnesses present; ProcessQueryWitness binding missing), #57 (Stochastic Conformance: WfNetConst base present; StochasticArcWeight/ImmediateTransition/TimedTransition not yet typed) |
| `MISSING_TYPE_LAW` | 0 | (cleared: #3 upgraded to PARTIAL; #6 upgraded to PARTIAL — OCPQ query structure implemented in src/ocpq.rs) |
| `DUPLICATE_OR_BACKGROUND` | 3 | #8 (PM4Py dup), #17 (YAWL BPMS), #19 (YAWL TM dup) |
| `OUT_OF_SCOPE_WITH_REASON` | 10 | #2, #4, #10, #12, #13, #15, #20, #22, #23, #24 |

**Total corpus scanned:** 47 papers (20 workflow/ + 4 other directories + 6 foundational additions + 6 new additions #45–#50 + 6 new additions #51–#56 + 5 new additions #57–#61)  
**6 papers with active PARTIAL type-law obligations** (#1, #3, #5, #48, #51, #57).  
**0 papers with fully missing type-law surfaces** — all MISSING entries have been addressed.  
**8 papers fully covered by type** (#11, #14, #18, #33, #34, #35, #47, #49) with compile-pass and compile-fail fixtures.

---

## Priority Missing Work

1. **#3 POWL 2.0 / Separable WF-nets** — `SeparableWfNet<SOUNDNESS>` marker type; `ChoiceGraph` arc type in `src/powl.rs`; `WfNet2Powl` conversion witness proving language preservation
2. **#6 OCPQ** — `ObjectTypeSet`/`EventTypeSet` as `ConstParamTy` params; `ConstraintViolation<ObjType, EvType>` typed result in `src/ocpq.rs`
3. **#1 PPM Compliance** — `ComplianceConstraintWitness<W>` binding prediction target to a named law; compliance score as `Between01<NUM, DEN>` metric
4. **#5 XES→OCED** — `XesToOcedProjection` with explicit `LossPolicy` and `LossReport` in `src/interop.rs`
5. **#14 Workflow Patterns** — Named `WorkflowPattern` enum as `ConstParamTy`; attach pattern coverage claim to `WfNetConst<SOUNDNESS>`
6. **#45 / #56 Heuristics Miner / Causal Nets** — `CausalNet`, `CausalBinding`, `InputBinding`, `OutputBinding` dedicated types in `src/causal_net.rs`; `DependencyMeasure` as `Between01<NUM, DEN>` const-generic fraction
7. **#46 Weske BPM** — `GatewaySemantics<KIND>` const-generic type in `src/bpmn.rs` binding `GatewayKind` variant to its semantic law (XOR routes one token, AND duplicates, OR activates subset)
8. **#48 Multi-Perspective PM** — `ResourcePerspective` and `DataPerspective` as `PhantomData` extension markers on `XesEvent` in `src/xes.rs`; perspective-scoped attribute typed surface
9. **#49 OC-PM Divergence/Convergence** — `DivergenceWitness` and `ConvergenceWitness` as unit-struct witness types in `src/witness.rs`; each certifies a structure has been assessed for the respective defect
10. **#51 Process Querying Methods** — `ProcessQueryWitness` named witness in `src/ocpq.rs` linking `OcpqQuery` to Polyvyanyy et al. 2017 framework; `TemporalPredicate` full temporal ordering axiom coverage
11. **#52 Event Abstraction** — `AbstractionLevel` marker type in `src/eventlog.rs` distinguishing low-level from high-level activity logs
12. **#53 Concept Drift Detection** — `DriftWitness` marker and `DriftKind` (`ConstParamTy` enum: Sudden/Gradual/Recurring/Incremental) in `src/prediction.rs`
13. **#54 Predictive PM survey** — `PredictionHorizon` and `PrefixLength` const-generic params in `src/prediction.rs`
14. **#55 MagTempMiner** — `TimedResponse`, `TimedPrecedence`, `TimedChainResponse` variants added to `DeclareTemplate` `ConstParamTy` enum in `src/declare.rs`
15. **#57 Stochastic Conformance** — `StochasticArcWeight<NUM, DEN>` newtype bounded by `Between01`; `ImmediateTransition` and `TimedTransition` as distinct unit-struct markers in `src/petri.rs`
16. **#58 OCEL Extraction/Materialization** — `ExtractionProjection<POLICY>` type with named `LossPolicy` in `src/ocel.rs` or `src/loss.rs`
17. **#59 Petri Net Unfoldings** — `BranchingProcess` and `UnfoldingPrefix` structural types in `src/petri.rs`
18. **#60 Mining Roles** — `RoleAttribute` and `ResourceGrouping` typed newtypes in `src/eventlog.rs`
19. **#61 Process Performance Analysis** — `PerformanceMetric<KIND, UNIT>` newtype and `PerformanceMetricKind` `ConstParamTy` enum in `src/conformance.rs`
